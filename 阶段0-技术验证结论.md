# 阶段 0 · 技术验证结论

**验证环境**：本机的 Microsoft Edge，页面用 `file://` 直接从磁盘打开，和你平时双击文件的情况一致。
**验证方式**：自动测试（`tests/phase0-tech-check.js`），5 项全部通过。

| 编号 | 验证内容 | 结果 | 结论 |
|---|---|---|---|
| T1 | 本地网页把数据存进浏览器，刷新后还在 | ✅ 通过 | 用它做**主存储** |
| T2 | 本地网页能用 IndexedDB，刷新后读得回来 | ✅ 通过 | 用它**保存备份文件的位置**（这个位置不能存在普通存储里） |
| T3 | 具备写文件的能力（`showSaveFilePicker`、`showOpenFilePicker`），且页面是安全上下文 | ✅ 通过 | **自动备份方案成立**，不需要退回手动导出 |
| T4 | 页面切到后台时，`Date.now()` 仍按真实时间走 | ✅ 通过（1.5 秒实测偏差在 0.4 秒内） | 倒计时可以放心用"结束时刻"计算 |
| T5 | 把网页挪到别的文件夹，数据仍在 | ✅ 通过 | 代码文件可以随便挪位置，不会丢数据 |

## 因此定下的方案

- **数据主存储**：浏览器本地存储（`localStorage`），键名 `jintian.v1`。
- **备份文件的位置**：存在 IndexedDB 里，这样关掉再打开不用重新选文件。
- **自动备份**：用浏览器写文件的能力，往你指定的那个文件里写。
- **倒计时**：记录"该结束的时刻"，每秒算一次还剩多少。不依赖定时器被不被降频。
- **代码文件位置**：随便放哪都行，数据跟着浏览器走，不跟着文件走。

## 一个需要你知道的前提

这套方案能成立，前提是你在**同一台电脑、同一个 Edge**里打开这个 App。换个浏览器就看不到数据了，那时候用备份文件恢复。
