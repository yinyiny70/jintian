# 今天 · 第一版 PRD

> 本文档是第一版的开发依据。后续讨论如果和本文冲突，以本文为准；要改，先说清改哪一条。

**文档状态**：需求已逐条确认；布局已看过可点击草稿并表示满意
**适用版本**：第一版（v1）
**界面语言**：中文

---

## 名词约定

后面反复用到的词，先在这里定死：

| 词 | 含义 |
|---|---|
| 任务 | 今天计划里的一件事，例如"写季度总结" |
| 预估时长 | 给任务提前填的分钟数，只是参考 |
| 实际用时 | 这件事真正被计时累计出来的分钟数 |
| 计时会话 | 点一次"开始"到这次计时结束之间的一段过程 |
| 专注时长 | 当天所有任务的实际用时之和 |
| 每日总结 | 一天一篇的整段文字 |
| 随手记 | 跟任务无关的一条短记录，写下时带时分 |
| 任务感受 | 挂在某个任务下的一句短文字 |
| 备份文件 | 放在你电脑上、由这个 App 自动写入的数据文件 |

---

## 1. 产品目标和使用场景

### 目标

做一个**只给自己用**的每日工具：把一天的事排出来，用倒计时推着我把每件事做完，做完之后留下一点当时的感受和当天的总结。

它要解决的真正问题不是"记住有哪些待办"，而是三件事：

1. 心里知道每件事大概要多久，但**不会真的盯着时间去做**。
2. 做完就过去了，**没留下任何痕迹**，一周以后想不起来时间花在哪。
3. 计划工具用过很多个，都变成"任务列表换个名字"，**多记一次反而更累**。

### 使用场景（典型一天）

- **早上**：打开页面，把今天要做的事写进「今日计划」，每条顺手填个预估时长，拖动排一下先后。
- **做事前**：点这件事的"开始"，输入这次打算做多久，开始倒计时。
- **做的过程中**：页面一直开着。倒计时在底部一直看得见，随时知道还剩多少。
- **时间到了**：响铃约 10 秒并弹出一个小框，问做完了没有。做完了打勾并写一句感受；没做完就加时间；不想做了就停下。
- **任何时候想到什么**：切到「备忘录」写一条随手记，几秒钟的事。
- **晚上**：在「备忘录」里写当天的总结，顺便看看今天每件事花了多久。
- **以后**：点「回看」里的日历，翻回某一天，看那天做了什么、花了多久、写了什么。

### 使用环境（硬性前提）

- 只在一台电脑的一个浏览器（Edge）里运行。
- **不登录、不联网、不部署、不做手机端。**
- 做事时页面一直开着——这是"到点响铃"能成立的前提。

---

## 2. 整体页面框架和导航结构

### 窗口骨架

整个 App 是一个窗口，分左右两块：

```
┌────────────────────┬──────────────────────────────────────────┐
│ 侧边栏 208px       │ 内容区（一次只显示一个页面）             │
│ 今天               │                                          │
│ 9月21日 星期一     │                当前页面                  │
│                    │                                          │
│ ▸ 首页             │                                          │
│ ▸ 今日计划         │                                          │
│ ▸ 备忘录           │                                          │
│ ▸ 回看             │   ────────────────────────────────────── │
│                    │   ● 读 30 页书   12:34    [暂停] [结束]  │
│ ▤ 已自动保存到文件 │   ────────────────────────────────────── │
│                    │          ↑ 计时条（仅计时中出现）        │
│                    │                                          │
│                    │                                          │
└────────────────────┴──────────────────────────────────────────┘
```

### 侧边栏（固定不动）

| 区域 | 内容 |
|---|---|
| 顶部 | App 名称「今天」+ 今天的日期和星期 |
| 中部 | 四个导航入口：首页 / 今日计划 / 备忘录 / 回看。当前页高亮 |
| 底部 | 备份状态条：显示"已自动保存到文件"、备份文件名、最后一次写入时间。**点它可以打开备份/恢复的小面板** |

App 名称「今天」是第一版的占位名，随时可改，改名不影响任何功能。

### 内容区

一次只显示一个页面，由侧边栏切换。页面**不做内部滚动条、不用弹层盖住主内容**，内容多了整页变长。

计时进行中时，内容区底部出现一条**计时条**：正在做的事、剩余时间、"暂停/继续"、"结束"。它在每个页面都会出现，这样切到备忘录或回看时也知道还剩多久。

### 两个弹框

都是盖在整个窗口上的居中面板，一次只出现一个。

**开始计时弹框**

```
开始计时
读 30 页书
这次打算做多久   [ 45 ] 分钟
                        [取消] [开始计时]
```

- 数字框默认填这条任务的**预估时长**，每次都可以改。
- 第一版只放一个数字框，不做常用时长快捷键。

**响铃弹框**

```
（时间到了）
读 30 页书
这次计时 45 分钟
[        做完了        ]
再加 [ 10 ] 分钟  [继续]
先停下
```

- 点"做完了"进入第二步：一句「这件事感觉怎么样？」+ 文本框，写不写都行，点"打勾完成"结束。
- "再加 N 分钟"是在原来基础上继续计时，**已经用掉的那段照样算进实际用时**。
- "先停下"结束这次计时，不打勾，**已经用掉的那段也算进实际用时**。

### 窄窗口（次要要求）

窗口变窄时，侧边栏自动变成顶部的一行导航，任务行上的图标按钮收起。第一版只要求"不破版、不重叠"，不追求手机上的体验。

---

## 3. 首页的内容结构

首页是**整个 App 里信息最少的一页**，只回答两个问题。它不做任何操作，也不显示别的数字。

### 没有在计时的时候

```
此刻
没有在计时
去排今天的事 →

────────────────────────

今天专注
1 小时 24 分
```

### 正在计时的时候

```
此刻
写季度总结
18:42
▬▬▬▬▬▬▬░░░░░░░

────────────────────────

今天专注
1 小时 24 分
```

### 规矩

- 只放两件事：**此刻在不在计时（在计时的话是哪件事、还剩多少）**，以及**今天一共专注了多久**。
- 不放：任务列表、完成数量、进度百分比、图表、总结入口、待办提醒。
- "今天专注"按秒实时变化，不用刷新。
- "去排今天的事"只是一条跳转链接，跳到「今日计划」，不是新功能。
- 页面垂直居中，留白大方。宁可空，不要塞。

---

## 4. 每个模块的用途

四个模块分工不同，谁也替不了谁。这是硬要求：**不允许任何一个模块退化成"任务列表换个名字"。**

| 模块 | 它存在的理由 | 一句话概括 |
|---|---|---|
| **首页** | 抬眼就知道现在是什么状态，不用找 | 一面镜子，只映出两件事，不产生数据 |
| **今日计划** | 把今天要做的事排出来，并给每件事一个时间预期和先后顺序 | 唯一能新增任务的地方 |
| **计时与提醒** | 用倒计时逼自己按时做完，顺手把真实用时记下来 | 长在任务上的动作，不是一个页面 |
| **备忘录** | 把当天发生的、想到的、感受到的写下来 | 以今天为轴的三本本子 |
| **回看** | 让过去的日子能翻回去看，否则写下来的东西等于没写 | 全部数据按日期的只读投影 |

### 计时与提醒为什么不是一个独立页面

计时必须**挂在一件具体的事上**，否则它只是个闹钟，记不下任何东西。所以它的形态是：

1. 每个未完成的任务行上有一个"开始"；
2. 点开后输入这次的时长；
3. 倒计时跑起来后，任务行本身变样（高亮、显示剩余时间、按钮变成"暂停"），同时底部出现计时条；
4. 到点响铃约 10 秒 + 弹框。

它没有自己的导航入口，因为它的全部动作都发生在任务上。

---

## 5. 每个模块第一版最必要的数据和操作

原则：**能少一个字段就少一个。** 下面列出来的都是"缺了它这个模块就不成立"的，没列的都不做。

### 5.1 今日计划 · 任务

| 数据 | 说明 | 怎么来 |
|---|---|---|
| 任务名称 | 一句话，例如"读 30 页书" | 我手填 |
| 预估时长 | 分钟数 | 我手填（默认 30） |
| 实际用时 | 累计的分钟数 | 计时自动累加，不能手改 |
| 是否完成 | 打勾 | 我点，或响铃后点"打勾完成" |
| 任务感受 | 一句短文字，可以空着 | 我在备忘录里写 |
| 顺序 | 在当天列表里的位置 | 我拖动 |

**操作**：添加任务（回车或点"添加"）、勾选完成/取消、拖动排序、改名、删除。

**任务行显示**：拖动把手、勾选框、任务名、`预估 45 分钟 · 实际 31 分钟`、改名、删除、"开始"。

- 已完成：名称加删除线，不再显示"开始"。
- 超出预估：多一个"（超了）"。
- 正在计时：整行高亮，`开始` 变成 `暂停/继续`，用时那栏变成剩余时间。

**页面顶部唯一一处汇总**：`9月21日 星期一 · 完成 2 / 5`。只此一处，不在任务行上再重复进度。

### 5.2 计时与提醒

| 数据 | 说明 |
|---|---|
| 所属任务 | 这次是在给哪件事计时 |
| 本次计划时长 | 点开始时输入的分钟数 |
| 剩余时间 | 实时倒数 |
| 实际用时 | 每次计时结束（不管怎么结束）都累加到任务上 |

**操作**：开始（输入时长）、暂停/继续、中途结束、响铃后三选一（做完了 / 再加 N 分钟 / 先停下）。

**行为要求**：

- 同一时间**只允许一个计时在跑**。给另一件事点"开始"时提示一句"现在还在给「某某」计时，要先结束吗"。第一版不做并行计时。
- 响铃约 10 秒，同时弹出响铃框。只要页面开着就能响。
- 刷新页面后，正在跑的计时**自动接着走**，已经过去的时间算数。
- 到点后如果一直不处理弹框，它会一直停在那儿，不会自己消失。
- ⚠️ 草稿里为了让你看到效果，倒计时是加速的。**正式版本按真实时间走。**

### 5.3 备忘录

三本本子在同一页，从上到下依次是：

| 本子 | 数据 | 说明 |
|---|---|---|
| **今日总结** | 正文 | 一天一篇，随写随存 |
| **随手记** | 写下时的时分 + 正文 | 一天可以很多条，跟任务无关 |
| **任务感受** | 挂在任务上的短文字 | 只列今天**有用时记录**的任务；没写过感受的留一个空框等着写 |

**操作**：写/改今日总结（自动保存，界面显示"已自动保存 · 15:42"）、新增一条随手记（回车或点"记下"）、删除一条随手记、给今天做过的任务就地写感受。

### 5.4 回看

| 数据 | 说明 |
|---|---|
| 哪一天有记录 | 日历上带小圆点的日期 |
| 当天做的事 | 任务名、实际用时、是否完成、写过的感受 |
| 当天的专注时长 | 当天所有实际用时之和 |
| 当天的总结 | 整段文字 |
| 当天的随手记 | 时分 + 正文 |

**操作**：在月历上点任意一天，右侧换成那天的全部内容。**只能看，不能改**（点是今天则提示去"今日计划"和"备忘录"里改）。

第一版只做当月，不做跨月翻页。

### 5.5 全局设置

只有一项，放在侧边栏底部的备份状态条里：

| 数据 | 说明 |
|---|---|
| 备份文件位置 | 第一次选定的那个文件，之后一直往它写 |
| 最后一次写入时间 | 显示给你看，让你确信它在工作 |

---

## 6. 首页、今日计划和各模块之间的关系

### 数据从哪来、往哪去

```
         （唯一能新增任务的地方）
               今日计划
                  │ 产生：任务、预估时长、顺序、完成状态
                  ▼
               计时与提醒 ─── 产生：实际用时（累加到任务上）
                  │                      │
                  │                      ▼
                  │                当天专注时长
                  │                      │
                  │                      ▼
                  │            首页（只读，只显示两件事）
                  ▼
        备忘录（三本本子：今日总结／随手记／任务感受）
                  │
                  ▼
          回看（按日期只读地看上面全部内容）
```

### 五条关系，逐条说清

1. **首页不产生数据。** 它显示的两个数字分别来自"当前的计时状态"和"当天所有任务的实际用时之和"。
2. **今日计划是唯一入口。** 任务只能在这里新增和修改。首页的"去排今天的事"、备忘录里的任务感受，都只是指回这里，不重复造一份任务列表。
3. **计时长在任务上，但状态全局可见。** 计时只能在任务行上发起；一旦跑起来，底部计时条在任何页面都能看到，任务行本身也会变样。两处显示的是同一份状态，不会各算各的。
4. **备忘录只写字，不管事。** 任务感受写在备忘录页，但它归属的是某个任务；每日总结和随手记归属的是某一天。
5. **回看是所有数据的另一个视角。** 它按日期把"任务 + 用时 + 感受 + 总结 + 随手记"组装成一天的样子，只读。今天在别处做的任何改动，点回今天就能看到最新的。

### 一条关键规则：每天都是干净的新一页

- 昨天没做完的任务**不会自动出现在今天**。想继续做，自己重新加一遍。
- 昨天的内容一个字都不会丢，只是需要点日历翻回去看。
- 所以"回看"不是附加功能，它是"每天清零"这个选择能成立的前提。

---

## 7. 本地数据文件、备份和恢复要求

### 两级保存

**第一级：浏览器本地**

- 所有数据存在这个浏览器自己的本地存储里。
- 刷新页面、关掉页面、重启电脑，数据都还在。
- 打开页面先读本地，页面几乎是瞬间出来的。

**第二级：备份文件（放在你电脑上的一个文件）**

- 第一次使用时，你指定一个文件位置，之后 App 自动往里写。
- 建议文件名：`今天备份.json`。扩展名就用 `.json`，纯文本，万一哪天 App 出问题，你用记事本打开它也能把内容抄出来。
- 备份内容：**全部数据**——所有日期的任务、实际用时、完成状态、任务感受、每日总结、随手记、备份设置本身。

### 什么时候写

- 数据发生变化后 1 秒内自动写一次（不用你点任何按钮）。
- 页面被关闭、被切到后台之前，再写一次。
- 侧边栏底部的状态条随时显示最后一次成功写入的时间。
- 侧边栏底部状态条点开，是一个小面板，里面三件事：
  1. 换一个备份文件位置
  2. 立刻备份一次
  3. **从文件恢复**

### 恢复怎么做

1. 打开备份面板 → 点"从文件恢复"。
2. 选一个备份文件。
3. **明确提示：这会覆盖当前的全部数据，且无法撤销。** 必须点确认才继续。
4. 恢复完成后，页面上的内容立刻变成文件里的内容。

### 必须诚实说明的风险

- 你在浏览器里点"清除浏览数据"，或者换浏览器、重装系统，**浏览器本地的数据会没**。这是浏览器的规矩，App 自己也救不回来。
- 遇到这种情况，靠那个备份文件就能全部拿回来。
- 换电脑也一样：把备份文件带过去，恢复即可。
- 浏览器出于安全考虑，**偶尔会要求你重新确认一次"还允许写这个文件吗"**，这是正常的，不是出了故障。

### 第一版不做

- 云端同步、多设备同步。
- 自动保留多份历史版本（只维护当前这一个备份文件）。

---

## 8. 第一版必须完成的功能

按模块列，每一条都是"做完才算第一版"。

**框架**

- [ ] 一个窗口，左侧固定导航（首页 / 今日计划 / 备忘录 / 回看），右侧内容区
- [ ] 侧边栏顶部显示 App 名和今天日期；底部显示备份状态
- [ ] 内容区底部常驻计时条（仅计时中出现）
- [ ] 中文界面，浅色为主并跟随系统深浅色
- [ ] 窄窗口不破版

**首页**

- [ ] 没有计时时显示"没有在计时"
- [ ] 计时中显示任务名和剩余时间，并按秒变化
- [ ] 显示"今天专注"，按秒实时增长
- [ ] 除以上两项外不显示别的信息

**今日计划**

- [ ] 添加任务（任务名 + 预估时长）
- [ ] 勾选完成 / 取消勾选
- [ ] 拖动调整顺序
- [ ] 改名、删除
- [ ] 任务行显示预估时长和实际用时；超额显示"（超了）"
- [ ] 页面顶部显示"日期 + 完成 N / M"

**计时与提醒**

- [ ] 每个未完成任务上一个"开始"
- [ ] 点开始弹出输入时长的框，默认填该任务的预估时长
- [ ] 倒计时正常走，任务行高亮并显示剩余时间
- [ ] 全局计时条，切页面时依然可见
- [ ] 暂停 / 继续
- [ ] 中途结束
- [ ] 到点响铃约 10 秒 + 弹出响铃框
- [ ] 响铃框三选一：做完了 / 再加 N 分钟 / 先停下
- [ ] "做完了"之后接一步写感受，然后打勾
- [ ] 实际用时累加到任务上；不管怎么结束都记账
- [ ] 刷新页面后，正在跑的计时自动接着走
- [ ] 同时只允许一个计时，冲突时给出提示

**备忘录**

- [ ] 今日总结：一天一篇，自动保存
- [ ] 随手记：新增（带时分）、删除、按时间顺序排列
- [ ] 任务感受：列出今天有用时记录的任务，可就地写
- [ ] 三本本子同页，从上到下：总结 / 随手记 / 任务感受

**回看**

- [ ] 当月日历，有记录的日期带小圆点，今天有标记
- [ ] 点某一天，右侧显示：那天做的事（含用时和感受）、当天专注时长、当天总结、当天随手记
- [ ] 过去的内容只读
- [ ] 没有记录的日期显示明确的空状态提示

**数据**

- [ ] 本地持久化：刷新、关页面、重启电脑都不丢
- [ ] 首次使用指定备份文件位置
- [ ] 数据变化后 1 秒内自动写入备份文件
- [ ] 从备份文件恢复，且恢复前有明确的覆盖确认

---

## 9. 第一版暂时不做的功能

这些都是**明确不做**，不是"忘了"。做了任何一条都算超出第一版。

- 登录、账号、密码
- 联网、云同步、多设备
- 部署上线、做成网站给别人访问
- 手机端、平板端适配
- 多人协作、分享、导出给别人看
- 未完成任务自动顺延到第二天
- 任务的分类、标签、优先级、子任务、截止日期
- 提醒我"该写总结了""该睡了"之类的任何额外催促
- 统计图表、周报月报、趋势分析
- 搜索功能
- 主题换肤、字体大小调节等外观设置
- 计时并行（同时跑多个）
- 常用时长快捷键
- 跨月翻看历史
- 编辑过去某一天的记录
- 备份多版本历史

---

## 10. 可以实际检查的验收标准

下面每一条都可以**动手试一遍**。试不过就是没做完。

### A. 打开与持久化

1. 打开页面 → 首页正常出现，页面上只有"此刻"和"今天专注"两组信息。
2. 在今日计划里加 3 件事并各填预估时长 → 刷新页面 → 3 件事原样还在，顺序不变，预估时长不变。
3. 完全关掉浏览器再打开 → 内容照旧。
4. 电脑重启后再打开 → 内容照旧。

### B. 今日计划

5. 在输入框里打字后按回车 → 任务被加进列表（点"添加"按钮同样有效）。
6. 把第 5 条拖到第 2 条的位置 → 松开后顺序改变 → 刷新页面 → 新顺序保持。
7. 勾选一条任务 → 名称出现删除线，"开始"按钮消失；取消勾选 → 恢复原样。
8. 给一条任务改名 → 刷新后仍是新名字。
9. 删除一条任务 → 刷新后不再出现。
10. 给一条任务填预估 20 分钟，实际计时到 30 分钟 → 那一行显示"（超了）"。

### C. 计时与提醒

11. 点某条任务的"开始" → 弹出输入框，数字框里预填的是这条任务的预估时长。
12. 把时长改成 1 分钟并开始 → 任务行高亮，显示剩余时间；底部出现计时条。
13. 切到「备忘录」再切到「回看」 → 计时条一直都在，剩余时间继续减少。
14. 点"暂停" → 剩余时间停止减少；点"继续" → 恢复。
15. 1 分钟到 → 响铃约 10 秒，同时弹出响铃框。
16. 选"做完了" → 出现感受输入框 → 写一句并点"打勾完成" → 任务被打勾、实际用时增加 1 分钟、首页"今天专注"增加 1 分钟、备忘录的"任务感受"里出现这句话。
17. 重来一次同样的 1 分钟计时，到点选"先停下" → 任务**没有**打勾，但实际用时同样增加了 1 分钟。
18. 重来一次，到点选"再加 2 分钟" → 继续倒数，且之前那 1 分钟已经算进实际用时。
19. 开始一个 5 分钟计时，等 30 秒后刷新页面 → 计时接着走，剩余时间约为 4 分 30 秒。
20. 给 A 任务开始计时，再去点 B 任务的"开始" → 会被拦下并提示当前还在给 A 计时。
21. 把计时条上的"结束"点掉 → 计时停止，任务行恢复原样，用掉的时间已记账。

### D. 首页

22. 没有任何计时时 → 首页显示"没有在计时"。
23. 开始计时 → 首页显示任务名和剩余时间，数字每秒在变。
24. 让两件事各产生一次用时 → 首页"今天专注"等于两者之和。

### E. 备忘录

25. 在今日总结里写几行字 → 关掉页面再打开 → 文字还在，并显示"已自动保存 + 时间"。
26. 添加一条随手记 → 显示写下时的时分 → 刷新后还在。
27. 删掉一条随手记 → 刷新后不再出现。
28. "任务感受"里只出现今天有过用时记录的任务；没写过感受的任务，框是空的但不是没有。

### F. 回看

29. 日历上，有记录的日期带小圆点，今天有单独标记。
30. 点一个有记录的过去日期 → 右侧显示那天做的事（含用时和感受）、当天专注时长、当天总结、当天随手记。
31. 点一个没有记录的日期 → 显示"这一天没有留下记录"。
32. 点今天 → 显示今天的最新内容，并提示去"今日计划"和"备忘录"里修改。
33. 在过去日期的界面里找不到任何可以打字或修改的地方。

### G. 备份与恢复

34. 第一次使用时，被要求指定一个备份文件的位置。
35. 加一件事 → 1 秒内，侧边栏底部状态条上的时间更新。
36. 用记事本打开那个备份文件 → 能看到刚加的那件事（纯文本，看得懂）。
37. 在浏览器里清除该站点的数据 → 重新打开页面 → 内容空了；用"从文件恢复"选那个备份文件 → 全部内容回来了。
38. 恢复前一定会先看到"会覆盖当前全部数据"的提示，必须点确认才会执行。

### H. 边界

39. 把窗口拖到很窄（约 320 像素宽）→ 页面没有横向滚动条，文字和按钮不重叠、不被切掉。
40. 把窗口拖宽到全屏 → 内容跟着变宽，不会挤在左边一小条里。

---

## 附录 A：绕不过去的硬性边界

这四条是浏览器本身的限制，不是没做好，第一版也不会去解决：

1. **页面必须开着才会响铃。** 浏览器关掉、电脑关机或睡眠，它不会响，也不会在后台替你计时——它没在运行。
2. **只认这台电脑的这个浏览器。** 换个浏览器就看不到数据，除非用备份文件恢复。
3. **清除浏览器数据会清掉本地数据。** 靠备份文件兜底。
4. **自动写备份文件时，浏览器偶尔会要求重新确认授权。**

---

## 附录 B：已经在讨论里定下来的默认设定

以下几条是在需求讨论时确认过的，写在这里防止后面又被改回去：

- 计时过程中允许暂停，允许提前结束。
- 刷新页面后，正在跑的计时会继续，不会白费。
- 过去的日子只能看，不能补写。
- 任务可以改名字、可以删除。
- 一天的结束以自然日为准（0 点换页）。
- 预估时长在添加任务时填写，之后可以改；开始计时时默认用它，也可以临时改。
- 响铃时长约 10 秒，同时弹框。

---

## 附录 C：留到开发时再顺手定的小事

这些不影响开工，做到那一步再定：

- 响铃具体用哪一种提示音（默认先用一个柔和的提示音）。
- App 名字「今天」是否要换。
- 备份文件是否希望额外保留最近几份副本。
