# 今天 · 外观说明

## 一条硬规矩：只参考材质，绝不参考内容

这一版外观的灵感来自 macOS「天气」App 的**材质、空间层级、光影、透明度和交互反馈**。

但**绝对不能出现任何气象元素**：天空、云、太阳、月亮、星星、雨、雪、天气图标、山川、城市天际线，以及任何能被读成"天气"的图形。

这个 App 是**个人工作与生活控制中心**，不是天气软件。背景里只有纯抽象的柔光，没有任何形状、没有地平线。

## 这一版改了什么

从"平面卡片"升级成 Liquid Glass（液态玻璃）：界面变成几块悬浮的、会透光、有厚度的玻璃板，底下的环境光能透过来。

**只改了 `styles.css` 一个文件，`app.js` 和 `index.html` 一行都没动。** 所以功能完全不受影响——改完跑全套测试，63 项全过。

## 具体是怎么做出来的

**1. 环境层（背景）**

中性灰底 + 四团很柔和的光斑（白 / 青 / 暖橙 / 灰紫），半径都开到 1300–1500 像素，所以过渡是长的、平顺的，不会出现一块一块的色斑。最后加一层很淡的暗角（vignette），让中间亮、四周沉下去。

这几团光是纯抽象的光，用来给玻璃提供"可以折射的内容"。

**2. 玻璃分三层，靠厚度差拉开层级**

| 层 | 用在哪 | 做法 |
|---|---|---|
| 面板 | 侧边栏、内容区 | 背景白 28%（浅色）、毛玻璃 blur(34px) saturate(180%) |
| 卡片 | 任务行、随手记、日历、输入框 | 比面板亮一档，"浮"在面板上 |
| 弹框 | 计时、响铃、备份、确认 | 最厚的一层 blur(44px)，外加暗底遮罩 |

**3. 边缘与厚度**

每块玻璃都有：1px 高光描边 + 内侧顶部一道高光 + 底部一条极淡的暗线。这一点是"玻璃感"的关键——没有这道高光，再透也只是半透明色块。

**4. 光影方向统一**

所有高光都在左上、暗线在右下，模拟光从左上打过来。全局一致，才不会显得乱。

**5. 交互反馈**

- 悬停：元素轻轻抬起 1px，影子变大
- 按下：缩到 97%
- 缓动曲线带一点回弹（`cubic-bezier(.22,1.2,.36,1)`），像真的按在玻璃上
- 复选框是自己画的：未勾选是玻璃小方块，勾选后渐变填充 + 手绘勾

**6. 细节**

胶囊形按钮、圆角方框复选框、细滚动条、聚焦时的青色光环、计时中那颗会呼吸的小圆点。

## 改外观时必须守住的东西

1. `index.html` 里的 **id** 和 **`data-act` 属性**是功能的接口，**不能改名**。
2. `app.js` 里用到的类名不能删（`.task`、`.nav-item`、`.page`、`.cal-day`、`.quick-item`、`.feel-item`……）。
3. **改完必须跑 `运行测试.bat`，63 项全过**，才算没碰坏功能。
4. 有两条测试专门盯排版，最容易踩：**深色模式**下文字和背景不能同色；**320 像素窄窗口**不能出现横向滚动条。
5. 首页有一个测试专门盯着"不许加东西"——首页的标题必须只有「此刻」和「今天专注」两组。

## 已知的取舍

- 毛玻璃依赖浏览器能力。万一某台机器不支持，会自动退化成更实的底色，**文字永远清晰**。
- 系统开启"减少透明度"时，同样自动退化成实底。
- 系统开启"减少动态效果"时，所有动画会自动关掉。
