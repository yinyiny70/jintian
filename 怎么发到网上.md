# 怎么发到网上（让别人能打开用）

## 先搞清楚：发上去之后是什么样

别人点开你的链接，会看到一个**空白的、他自己的**「今天」——他自己的任务、他自己的总结，存在**他自己的浏览器**里。

他看不到你的任何记录，你也看不到他的。每个人用起来都像自己的私人工具。

这就是你要的 A 方案：**发的是这个工具本身，不是你的日记。**

## 你只需要上传一个文件夹

```
网页版\
    index.html      ← 整个 App 都在这 4 个文件里
    styles.css
    app.js
    favicon.svg
```

**只上传这个 `网页版` 文件夹里的东西。** 项目里其他东西（文档、测试、Git 仓库、你的备份文件）都不要传。

---

## 推荐做法：Netlify Drop（最快，2 分钟，不要钱）

1. 打开浏览器，访问 **`app.netlify.com/drop`**
2. 把**整个 `网页版` 文件夹**用鼠标拖进那个虚线框里（拖文件夹，不是拖里面的文件）
3. 等十来秒，页面上会出现一个链接，长这样：
   `https://随机名字.netlify.app`
4. **这就是你的网址**，点开就能看，发给人就能用

到这里就能用了。如果想把网址固定下来、以后能自己改：

5. 用邮箱注册一个免费账号（页面上会提示），然后把那个站"认领"到你的账号下
6. 想换一个好记的名字：站点设置里可以改前缀，变成 `https://今天-xxx.netlify.app` 之类

**花了多少钱**：0 元。

---

## 备选做法

### 备选一：Cloudflare Pages（免费额度更大，稍稳一点）

1. 注册 Cloudflare 账号（免费）
2. 左侧选 **Workers & Pages** → **Create** → **Pages** → **Upload assets**
3. 起个名字，把 `网页版` 文件夹里的 4 个文件拖上去 → **Deploy**
4. 得到 `https://名字.pages.dev`

好处：免费流量额度极大，个人项目根本用不完。适合以后你想长期放着。

### 备选二：GitHub Pages（最"正统"，步骤多一点）

你的项目本来就是 Git 仓库，所以这条路对你来说不算陌生：

1. 在 GitHub 上新建一个**公开**仓库，比如叫 `jintian`
2. 把 `网页版` 里的 4 个文件传上去（网页上直接拖文件也行）
3. 仓库 **Settings → Pages** → Source 选 `main` 分支、根目录 → Save
4. 得到 `https://你的用户名.github.io/jintian/`

---

## 国内访问速度这件事，要提前知道

上面几个都是国外服务。国内多数网络**能打开，但可能偏慢**，个别网络环境下会打不开。如果你的观众主要在国内、而且很在意打开速度，那就得走国内的服务（Gitee、阿里云 OSS、腾讯云 COS），但那通常要求**实名认证**，想绑自己的域名还要**备案**——那就和"不费时间不费钱"冲突了。

**我的建议**：先按上面的免费方案发出去。要是发给朋友后普遍反馈"打不开"或者"太慢"，再回来找我，我们换国内方案。别一上来就为这件事花时间。

---

## 上线之后你要知道的三件事

1. **数据是各人的。** 别人打开是空的，他们写的东西也只在他们的浏览器里。你这边（双击桌面「今天」那个）完全不受影响。
2. **你改了 App，要重新上传一次。** 网站上的文件是"一次上传的副本"，不会自动跟着你本机变。流程：我改完 → 你跑一次测试 → 我把 `网页版` 文件夹更新一遍 → 你再拖一次上去。
3. **别把项目文件夹整个传上去。** 里面有你的备份数据、全部文档和 Git 历史，传上去等于把底裤挂在门口。

---

## 常见问题

**Q：链接能关掉吗？**
能。Netlify / Cloudflare 后台都能一键下线或者删站。

**Q：别人能看到我在上面写了什么吗？**
不能。他们的数据和你的数据是两套，各自存在各自的浏览器里。

**Q：手机上打开能用吗？**
能打开，界面会自动变成上下结构。但这个 App 第一版明确没做手机适配，**体验不如电脑**，窄屏下有些按钮会收起来。想认真做手机版，跟我说。

**Q：要花钱吗？**
不花。免费子域名够用。想要好记的域名（比如 `jintian.com`）才要买，`.com` 大约 70–90 元一年，`.cn` 大约 30 元一年，这一步**完全可选**。

**Q：我以后想自己更新网站内容怎么办？**
最简单：重新拖一次 `网页版` 文件夹上去覆盖。Netlify 和 Cloudflare 都支持。
