# 怎么发到网上（GitHub + Cloudflare，一次配置，以后自动更新）

## 先搞清楚：发上去之后是什么样

别人点开你的链接，看到的是一个**空白的、他自己的**「今天」——他自己的任务、他自己的总结，存在**他自己的浏览器**里。

他看不到你的任何记录，你也看不到他的。每个人用起来都像自己的私人工具。

**发的是这个工具，不是你的日记。**

---

## 为什么用 GitHub 当源

直接把文件夹拖上去也行，但**每次改了 App 都要手动重传一遍**。

用 GitHub 当源之后，流程变成：

> 我改完 → 你双击一次 `发布到网上.bat` → **一到两分钟，网站自己更新好**

不用登录托管后台，不用重新拖文件。

---

## 一次性准备：三步

### 第 1 步：在 GitHub 建一个空仓库

1. 打开 `github.com`，登录（没有账号就先注册一个，免费）
2. 右上角 **+** → **New repository**
3. Repository name 填：**`jintian`**
4. 选 **Private（私有）**
5. **下面三个都不要勾**（Add a README / .gitignore / license）——要一个空仓库
6. 点 **Create repository**

> Private 就够了。Cloudflare 连私有仓库也是免费的，而且你的文档、测试、开发记录都不用公开。

### 第 2 步：把这个项目推上去

回到项目文件夹，**双击 `发布到网上.bat`**。

它会问你两件事：

```
你的 GitHub 用户名：
仓库名（直接回车 ＝ jintian）：
```

照填，回车。

接着会**弹出浏览器让你登录 GitHub**，登录一次以后就不用了。然后它自己把项目推上去。

看到窗口里出现 **「推送成功！」** 就对了。

### 第 3 步：让 Cloudflare 接上这个仓库

回到 Cloudflare 那个页面（就是你截图那一屏），点 **Connect GitHub**：

1. 授权 Cloudflare 访问你的 GitHub（会跳一个授权页，点同意）
2. 选中你刚建的 **`jintian`** 仓库
3. 配置这一页**只有一处要注意**：

   | 项目 | 填什么 |
   |---|---|
   | Framework preset | **None** |
   | Build command | **留空** |
   | Build output directory | **`网页版`** ← 关键，别填错 |

4. 点 **Save and Deploy**，等一到两分钟
5. 拿到网址，形如 `https://jintian.xxxx.pages.dev`

---

## 为什么输出目录要填 `网页版`

你的仓库里除了 App，还有文档、测试、Git 历史。填 `网页版` 之后，**网站上只会出现那 4 个文件**，其他东西一律不会被发布出去。

（这个文件夹是 `发布到网上.bat` 每次自动从根目录复制生成的，你不用管它。）

---

## 以后每次更新网站

1. 我改完代码
2. 你跑一次 `运行测试.bat`（确认 67 项全过）
3. 双击 **`发布到网上.bat`**，填一句"这次改了什么"
4. 等一到两分钟，网站自动更新

**就这三下。**

---

## 出问题时的对照表

| 现象 | 什么意思 | 怎么办 |
|---|---|---|
| 双击后说"还没关联 GitHub 仓库" | 正常，第一次都这样 | 按它提示填用户名和仓库名 |
| 推送时报错、说找不到仓库 | GitHub 上那个仓库还没建 | 先建空仓库（第 1 步） |
| 弹浏览器让你登录 | 正常的授权 | 登录一次，以后免了 |
| 网站上打开是 404 | 输出目录填错了 | 回 Cloudflare 把 Build output directory 改成 `网页版` 重新部署 |
| 网站上出现一堆 md 文件 | 输出目录填成了仓库根目录 | 同上，改成 `网页版` |
| Cloudflare 说连不上私有仓库 | 少见 | 把仓库改成 Public（文档里没有敏感信息），或者告诉我换方案 |
| **推送时报 `Recv failure: Connection was reset` / `Connection timed out`** | **跟仓库、用户名没关系**，是国内连 github.com 时通时不通，网络把连接掐了 | **把代理开上**（最好开成"全局 / TUN 模式"，git 才会走代理），再双击一次 `发布到网上.bat`。还不行就告诉 Codex 代理软件里那个"本地端口"（常见 7890 / 10809 / 1080），给 git 单独设一条走代理的配置 |

**卡住就把窗口截图发我。**

> 2026-09-23 真的遇到过：没开代理时报 `Recv failure: Connection was reset`，**开了代理一次就推上去**。
> 想确认到底推上去没有，有两个土办法：双击一次 `发布到网上.bat`，如果显示"没有新改动，直接推送"并且不再报错，
> 说明上一次其实已经成功；或者打开 GitHub 网页，看最新那条提交是不是你刚写的那句说明。

---

## 国内访问速度这件事

Cloudflare 在国内多数网络能打开，但**不算快**，个别网络环境下会打不开。这是国外服务的通病，不是你的配置问题。

先这样发出去用。如果朋友普遍反馈"打不开"或者"太慢"，再回来找我换国内方案——但那要实名认证、可能还要备案，和"不费时间不费钱"冲突，别一上来就为它花时间。

---

## 常见问题

**Q：别人能看到我在上面写了什么吗？**
不能。他们的数据和你的数据是两套，各自存在各自的浏览器里。

**Q：链接能关掉吗？**
能。Cloudflare 后台一键下线或删站。

**Q：手机上打开能用吗？**
能打开，界面会自动变成上下结构。但第一版明确没做手机适配，**体验不如电脑**，窄屏下有些按钮会收起来。想认真做手机版，跟我说。

**Q：要花钱吗？**
不花。免费子域名够用。想要好记的域名（如 `jintian.com`）才要买，`.com` 约 70–90 元一年，**完全可选**。

**Q：我不想用 Cloudflare 了怎么办？**
换成 Netlify 或 Vercel 连接同一个 GitHub 仓库就行，代码不用动。
