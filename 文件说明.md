# going的app · 文件说明

这个文件夹是这个 App 的全部资料，以后相关的东西都放这里。

> **每次重新开始之前，先看「项目状态.md」** —— 那里写着做到哪一步了、下次从哪继续。

## 项目放在哪（先说清，免得找错）

整个项目在：

```
C:\Users\Lenovo\Desktop\going的app
```

注意这个路径里有 "Desktop"，但它**不是**你桌面上看到的那个文件夹——你的桌面其实是 `D:\桌面`。之所以放在这里，是因为只有这个位置我才能正常读写、随时改代码。

**你平时不用管这个路径**：双击桌面上的「今天」图标就能用 App。那个图标只是个入口，指向这里。

## 你平时会碰到的，就一个

| 文件 | 是什么 |
|---|---|
| **index.html** | **就是 App 本身。双击它就能用。** |
| `favicon.svg` | 浏览器标签页上那个小图标 |
| `quotes.js` | 首页金句库（161 条，内置不联网） |
| `config.js` | 云端地址配置。部署完后端之后，把 Worker 网址填在这里 |

其余文件都是它的零件和资料，你不用打开。

## App 的零件

| 文件 | 是什么 |
|---|---|
| `styles.css` | 外观（颜色、间距、排版） |
| `app.js` | 全部逻辑（计划、计时、备忘、回看、备份） |

数据**不在**这些文件里，在浏览器和你的备份文件里。所以更新代码不会弄丢数据。

## 说明文档

| 文件 | 是什么 |
|---|---|
| **项目状态.md** | **每次开工先看这份**：做到哪一步、下次从哪继续、有哪些规矩 |
| **怎么发到网上.md** | 想把它发布成一个网址给别人用：照着点就行，免费 |
| **怎么开启账号功能.md** | 想开启账号登录和云同步：建数据库、建 Worker、填地址，四步 |
| **使用说明.md** | 怎么用这个 App，怎么看备份，有哪些限制。**建议先看这份** |
| **外观说明.md** | 界面用的是什么设计语言、怎么做的、以后改外观要注意什么 |
| **PRD.md** | 产品说明书，写清做什么、不做什么、验收标准 |
| **开发计划.md** | 十个阶段怎么走、每步怎么验 |
| **测试报告.md** | 114 项测试的结果，以及需要你手动验一下的那一步 |
| **第四版需求确认.md** | 第四版（字体 / 侧边栏 / 积分乐园）当时确认下来的需求 |
| **怎么开启账号功能.md** | 账号 + 云同步的后端怎么部署（Cloudflare，免费）。卡住时看里面的对照表 |
| **阶段0-技术验证结论.md** | 动手前先验证浏览器能力的那份结论 |
| `daily-planner-pages.html` | 动手前你确认过的那份页面草稿（能点的样子货），留个纪念 |

## 测试

| 文件 | 是什么 |
|---|---|
| **运行测试.bat** | 双击就跑全部 114 项测试，结果打在窗口里。**不会碰你平时用的数据** |
| `tests/` | 测试代码。`tests/run.js` 是执行器，`phase0` 到 `phase19` 是各阶段的测试 |
| `server/` | 账号功能的后端：`worker.js`（全部逻辑，部署时整个粘进 Cloudflare）、`schema.sql`（建表语句） |

## 部署后端时会用到的两个

| 文件 | 是什么 |
|---|---|
| **复制后端代码.bat** | 双击它，后端代码就整段进了剪贴板，直接粘进 Cloudflare 编辑器即可（不用手动全选，不怕漏） |
| `tools/copy-worker.js` | 上面那个按钮背后的脚本，平时不用管 |

另外：把 Worker 的网址直接粘到浏览器打开，会看到一个**中文自检页**，告诉你数据库绑定、表建好没有。

## 要发布到网上时，传这个文件夹

| 文件 | 是什么 |
|---|---|
| **网页版/** | **发布时把这个文件夹整个传上去**（里面是 App 那 4 个文件的拷贝）。别把整个项目传上去——里面有你的备份数据、全部文档和 Git 历史 |

改完 App 之后，让我把这个文件夹重新生成一次，再传一遍就行。

只在测试失败时才会多出一个 `tests/_failures/` 文件夹，里面是当时的界面截图，方便定位问题。

## 版本管理（Git）

| 文件 | 是什么 |
|---|---|
| _**Git怎么用.md**_ | 仓库现状、怎么存档、怎么改署名、出错了怎么办。**想自己操作之前看一眼** |
| **创建桌面快捷方式.bat** | 双击一次，桌面上就会出现「今天」图标。**第一次用之前点一下** |
| **保存版本.bat** | 双击它给当前状态留一个存档点。以后每次改完都可以点一次 |
| **发布到网上.bat** | 双击它把改动推到 GitHub，网站一两分钟后自动更新 |
| `初始化Git仓库.bat` | 建仓库用的。仓库已经建好了，平时用不到它；只在需要重新设置名字邮箱时才用 |
| `tools/` | 上面两个按钮背后的脚本，平时不用管 |
| `.gitignore` | 告诉 Git 哪些东西不用管（比如你的备份数据文件、测试失败截图） |

## 现在是第几步

第一版**已经做完**：需求 → 设计 → PRD → 开发 → 测试，61 项全过。

剩下唯一需要你亲手做一下的，是「使用说明.md」和「测试报告.md」里都提到的那一步：点开系统窗口选一个备份文件位置。因为那个系统窗口没法用程序自动点。

Git 仓库也已经建好，并且做了第一次存档（`d3a9697`）。以后想自己留存档点，双击「保存版本.bat」即可；详情见「Git怎么用.md」。

## 现在的习惯

- 想改界面、改文案、加功能 → 直接跟我说。
- 如果你自己动手改了 `PRD.md` 里的内容，跟我说一声，我以你改的为准。
