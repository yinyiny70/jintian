# 今天 · 项目状态与交接

> **这份文件是给"下次开工"用的。**
> 下次只要跟我说一句「继续改今天这个 App，先读 项目状态.md」，我就不会从零开始问你。
> 最后更新：2026-09-22 晚

---

## 一、这个项目是什么（一句话）

一个**只给自己用**的本地网页工具：排今天的事 → 用倒计时盯着自己做完 → 做完留一句感受 → 晚上写总结 → 以后能翻回任意一天。

纯本地，不联网、不登录、不部署、不做手机端。**它不是天气软件，界面只是借用了 macOS 天气 App 的材质语言。**

---

## 二、东西都在哪（三个位置，别搞混）

| 位置 | 是什么 | 注意 |
|---|---|---|
| `C:\Users\Lenovo\Desktop\going的app` | **项目本体**：App、文档、测试、Git 仓库 | 路径里有 "Desktop"，但**不是**你桌面上那个文件夹 |
| `D:\桌面\going的app\今天备份.json` | **你的数据**（App 每次改动自动写进去） | **千万别删**，删了就只能靠浏览器里那份 |
| `D:\桌面\今天.lnk` | 桌面上的「今天」图标 | 只是个入口，指向项目本体 |

为什么项目在 C 盘：**我的运行环境只有 C 盘用户目录能读写**，D 盘写不了。所以本体放 C 盘，桌面放快捷方式。

---

## 三、今天做完了什么

**第一版 · 功能**（63 项自动测试全过）

- 四个板块：首页 / 今日计划 / 备忘录 / 回看
- 首页极简：只有"此刻在不在计时"和"今天专注多久"两组信息
- 计时：点任务上的「开始」→ 输入这次多久 → 倒计时 → 响铃约 10 秒 → 弹框三选一（做完了 / 再加 N 分钟 / 先停下）；不管怎么结束，用掉的时间都记账
- 备忘录：今日总结、随手记、任务感受，三本本子一屏
- 回看：点日历任意一天，看那天的任务、用时、感受、总结、随手记（只读）
- 每天干净的一页；昨天没做完的不会自动顺延
- 数据存在浏览器里 + 自动写进你指定的备份文件 + 能从文件恢复

**第二版 · 外观**

- 升级成 macOS Liquid Glass（液态玻璃）：环境柔光、三层玻璃、边缘高光、光影方向统一、按下有回弹
- **只改了 `styles.css`**，功能代码一行没动，测试仍 63 项全过

**Git 存档**

```
4162c59  日常存档
d53bdb6  第一版：今天 App 完成，61 项测试全过
```

**第三版 · 发布准备（网页版）**

- App 现在能分清"本机版"和"网页版"：本机版第一次照样引导你设置备份；网页版不再弹窗，改成首页一行说明"数据只存在你这台设备"
- 加了 favicon，浏览器标签上有个小图标
- 生成了 `网页版/` 文件夹（上传就用它）
- 新增 4 项网页版测试，**总计 67 项全过**
- 本机版功能**一点没变**

**第四版 · 发布方式改成 GitHub 驱动（2026-09-22）**

- 加了 `发布到网上.bat`：一键把改动推到 GitHub，Cloudflare 自动重新部署
- 上线步骤全部改写进 `怎么发到网上.md`（GitHub 空仓库 → 推送 → Cloudflare 连接仓库 → 输出目录填 `网页版`）
- 为什么不用"拖文件夹上传"：那样每次改完都要手动重传，麻烦

**第五版 · 第三版的五条需求（2026-09-22 完成）**

1. ✅ 添加任务时不再填预估时长；预估改到任务行的编辑状态里设；没设预估时计时弹框默认 25 分钟
2. ✅ 单件事的用时精确到秒（`3 分 12 秒`）；总时长保持到分
3. ✅ 首页加了金句（161 条，古诗 / 名言 / 歌词），每 10 分钟换一句；计时中首页只剩倒计时
4. ✅ 不再跟随系统深浅色，默认阳光浅色（暖奶油底）；深色改成暖深咖；侧边栏底部有浅色/深色/跟随系统切换
5. ✅ 账号注册登录 + 云同步（用户名+密码，多用户，本机版也能登录同步，断网可继续记）

**测试：90 项全过**（原来 67 项，新增 16 项第三版功能 + 7 项后端自检）

**这一步之后还差一件事：把后端部署到 Cloudflare。**

步骤写在 `怎么开启账号功能.md` 里（建数据库 → 建 Worker 粘代码 → 绑定数据库 → 把网址填进 `config.js`）。
部署完把那个网址发我，我帮你填 `config.js` 并重新发布网页版。

**第六版 · 账号部署排错（2026-09-22，进行中）**

部署后端点「注册」时报「连不上服务器」，为此做了两件事：

1. **后端加了自检页**：把 Worker 网址直接粘到浏览器打开，它会自己检查「服务跑没跑起来 / 数据库绑定（变量名必须是 `DB`）有没有 / 四张表在不在」，哪条不对就写出该怎么办
2. **报错变成人话**：服务器内部真出问题时，App 里显示具体原因，不再笼统地说「连不上服务器」
3. 加了 **`复制后端代码.bat`**：双击一下，整段后端代码进剪贴板，粘进 Cloudflare 就不会漏
4. 修掉一个真问题：`config.js` 填上真实网址后，**测试页面会转头去连线上服务器**，导致第 13 阶段 5 项全挂。现在测试强制走本机服务器，并加了一项规定来守住这件事

**测试：91 项全过**（新增第 14 阶段 8 项）

**部署时真碰上的两个坑（都已修）**

1. 数据库绑定没跟着重新部署 → 自检页确诊，重新部署后绑定生效（自检页现在显示"一切正常"）
2. 密码加密参数撞了两条平台限制 → 原来写 120000 次迭代：Cloudflare 硬上限是 100000，且免费版每请求只有 10ms CPU（实测 100000 次要 18.5ms）。现已改成 **20000 次（≈3.7ms）**，并加了测试卡住这个数

**下一步**：重新部署一次后端（版本号会变成 `2026-09-22.2`），然后在 App 里注册账号试试。

**GitHub 仓库地址（发布用）**

```
https://github.com/yinyinY70/jintian.git
```

⚠️ 注意用户名是 **`yinyinY70`**（中间是 yin，末尾 Y 大写）。第一次填成了 `yinyy70`，
少了字母又改了小写，GitHub 就回"找不到仓库"——这类错误肉眼很难发现，
所以 `发布到网上.bat` 已经改成**让你直接粘贴地址**，不再手打用户名。

---

## 四、下次开工第一件事

**▶ 接着这里做（2026-09-22 收工时停在这一步）**

### 第四版已完成（2026-09-23）✅

1. **首页那句字分三种字体**：古诗＝华文行楷、歌词＝楷体、名言保持微软雅黑；
   每 10 分钟换一句不变，计时中首页只剩倒计时不变
2. **侧边栏那个备份框收起来了**：功能一个没删，缩成了一个小按钮（悬停能看到状态），
   旁边并排一个新加的「小铺」
3. **积分乐园**：每天签到 +5 / 专注满 5 分钟 +3 / 完成满 5 个任务 +5（各一次，一天上限 13）；
   23 件东西可换——8 个角落摆件（30 分）、6 套主题配色（80 分）、5 种金句底纹（25 分）、
   4 种金句花边（25 分）；**一律买断，永久归你**；摆件最多同时摆 3 个

**测试：114 项全过**（新增第 15—19 阶段共 20 项）。修掉一个测试自己的坑（清缓存前要先离开
App 页面，否则 App 会把旧数据写回去）。

> **下一步（待用户决定）**：第四版就到这里。之前卡住的"账号云同步"还等域名那件事——
> 用户说"等过段时间再买域名"。买好之后按 `怎么开启账号功能.md` 走：加自定义域 → 改 `config.js` → 发布。

> ⚠️ **有一处临时代码要记得删**：小铺页面最下面有个「试玩用：先给我加 1000 分」按钮
> （`index.html` 里的 `.dev-note` 那一块 + `app.js` 里 `act === "dev-boost"` 那一段）。
> 是 2026-09-23 给用户试玩加分的。**用户说看够了就删掉**——删掉按钮不影响他已经加到的分。

**❗ 最新结论（2026-09-22 晚）：卡在这里不是代码问题，是网络。**

用 `账号连接诊断.html` 连跑两次，数据很干净：

| 检查项 | 结果 |
|---|---|
| 访问百度（一定打得开） | 通了，131 毫秒 |
| 只连 Worker 那个域名、不读内容 | **卡住 8 秒，一点回应都没有** |
| 服务器自检 GET（连试 3 次） | 8004 / 5056 / 232 毫秒，全没通 |
| 本机跑一次密码加密 | 21 毫秒（完全不慢） |

也就是说：**`*.workers.dev` 这个域名，不开代理时根本连不上**。用户"平时偶尔用代理"，
之前那几次能看到自检页、能收到服务器回的中文报错，都是代理开着的时候。
后端代码、数据库、绑定、跨域许可，全都是好的。

**三条可选的路（等用户挑）**

1. **买个便宜域名绑到 Cloudflare**（约 ¥30/年，推荐）：给后端一个自己的域名。国内访问
   Cloudflare 的普通域名通常没问题（用户天天用的 Cloudflare 后台就是 Cloudflare 自己的）。
   步骤：买域名 → 加到 Cloudflare → 改 NS → Workers 里加「自定义域」→ 新网址填进 `config.js`
   → 双击 `发布到网上.bat`。
2. **完全不花钱，但要重写一轮**：把后端搬到国内能访问的免费服务（比如腾讯云开发 CloudBase
   的免费额度）。部署方式完全不同，后端要重写 + 重新测。
3. **什么都不改，用的时候开代理**：本机 App 不联网也能正常用，只有"登录 / 同步"那一下需要代理。
   缺点是用户自己得开着，朋友也得开。

> 不管选哪条，**本机 App 现在都能正常用**（排计划、计时、备忘、回看、存文件、恢复，全不依赖网络）；
> 被挡住的只有"账号 + 云同步"这一块。

后端已经部署成功。**剩下两件事，第一件 3 分钟：**

1. **验一下新代码真的上去了**
   浏览器打开 `https://jintian-api.3030370958.workers.dev`，看第一个绿框里那行浅色小字：
   - `版本 2026-09-22.2` → 对了，进第 2 步
   - 还写着 `.1` → 回 Cloudflare 那个 Worker，双击 `复制后端代码.bat`、粘贴、再点「部署」

2. **去 App 里注册账号**
   打开 App → 按 `Ctrl+Shift+R` → 左下角「点这里登录」→ 用户名 `Going`、密码 6 位以上 → 「注册一个新账号」
   - 成了：它会问"要不要把本机记录传上去"，选**传上去**
   - 提示"用户名已经被用了"：说明之前那次成了，直接点「登录」
   - 又报错：报的现在是具体原因，截图发我

3. **注册成功后收尾**（各双击一次）：`发布到网上.bat`（让网页版也带账号）、`保存版本.bat`（存档）

> 卡住过的地方：部署那一步 Cloudflare 后台报过一次 `API Request Failed: PUT ...`，重试后成功。
> 再遇到同样的红字，多半还是网络抽风，原样再点一次「部署」（**别刷新页面**，一刷新粘贴的代码就没了）。

**如果第二版外观还没存档**：双击 `保存版本.bat`，消息写 `第二版：Liquid Glass 界面`。

判断方法：双击 `保存版本.bat`，如果它说"没有新改动要存档"，那就是已经存过了。

**如果想把网页版发出去**：照 `怎么发到网上.md` 走。三步：GitHub 建空仓库 → 双击 `发布到网上.bat` → Cloudflare 连接仓库（输出目录填 `网页版`）。0 元。

---

## 五、下次可以做什么（候选，你挑）

**外观方向**

- 首页内容少时，下方空得比较多，可以把内容垂直位置调一调
- 主色现在是墨绿，可以试别的颜色
- 列表密度（更紧凑 / 更宽松）
- 页面切换加一点过渡

**功能方向**

- 日历翻上个月（第一版明确没做）
- 删任务前先问一句（防误删）
- 换一个响铃声音、或者调音量
- 任务支持"重复"（每天固定要做的事，一次设置天天生效）
- 键盘快捷键（比如回车直接开始计时）
- 把某一天的内容导出成一段文字
- 一周视图 / 简单的专注时长统计

---

## 六、几条"只有你和我知道"的规矩

1. **我改不了 `.git`**（环境的安全策略：禁止 AI 写版本库目录）。所以**存档这一步必须你双击 `保存版本.bat`**。
2. **我写不了 D 盘**。所以项目本体在 C 盘，你的桌面只能放快捷方式。
3. 我每次要动项目文件夹，系统会弹一次**授权提示，点同意**就行。
4. **改完必须跑 `运行测试.bat`，63 项全过**，才算没弄坏功能。测试不会碰你平时的数据。
5. **硬规矩**：外观只借 macOS 天气 App 的**材质**，绝不出现天空、云、太阳、月亮、星星、雨雪、天气图标、山川、城市天际线——任何气象元素都不行。
6. 你的备份文件是一层保险：**万一数据出事，用「从文件恢复」能全拿回来**。

---

## 七、下次怎么开场最省事

对我说这一句就够了：

> **「继续改今天这个 App，先读 项目状态.md」**

我会按这个顺序把状态捡起来：

1. 读 `项目状态.md`（这份）
2. 读 `外观说明.md`（改外观时）或 `PRD.md`（改功能时）
3. 跑一次 `运行测试.bat`，确认基线是好的
4. 然后直接问你要改哪一条

---

## 八、文件夹索引

| 文件 | 什么时候看 |
|---|---|
| **项目状态.md** | 每次开工第一份（就是这份） |
| **怎么发到网上.md** | 想发布成网址给别人用，照着点 |
| **发布到网上.bat** | 改完东西之后双击它，网站自动更新 |
| **使用说明.md** | 想知道 App 怎么用、怎么备份 |
| **外观说明.md** | 想改外观，或想知道为什么这么设计 |
| **PRD.md** | 想知道"到底要做什么、不做什么" |
| **开发计划.md** | 想知道当初是怎么一步步做出来的 |
| **测试报告.md** | 想确认测过哪些、修过哪些 bug |
| **Git怎么用.md** | 存档、回退、改署名 |
| **文件说明.md** | 所有文件的索引 |
| **阶段0-技术验证结论.md** | 当初动手前验过哪些浏览器能力 |
| `index.html` `styles.css` `app.js` | App 本体 |
| `favicon.svg` | 浏览器标签页小图标 |
| `网页版/` | 发布时要上传的那个文件夹 |
| `tests/` `tools/` | 测试代码，和几个双击按钮背后的脚本 |
