# 测试报告

**运行时间**：2026-09-22
**运行方式**：在你自己这台电脑的 Edge 上，用真实的浏览器把 App 跑起来，一项一项点着验的
**结果**：**114 项全部通过，0 项失败**

## 一共测了什么

| 阶段 | 内容 | 项数 | 结果 |
|---|---|---|---|
| 0 | 技术验证（能不能存住、能不能写文件） | 5 | 全过 |
| 1 | 骨架与导航 | 5 | 全过 |
| 2 | 数据层（刷新 / 关浏览器 / 重启后还在） | 6 | 全过 |
| 3 | 今日计划（添加、勾选、拖动、改名、删除） | 7 | 全过 |
| 4 | 计时与提醒（倒计时、响铃、三种收尾方式） | 12 | 全过 |
| 5 | 首页 | 4 | 全过 |
| 6 | 备忘录（总结 / 随手记 / 任务感受） | 4 | 全过 |
| 7 | 回看（日历 + 当天详情） | 5 | 全过 |
| 8 | 备份与恢复 | 7 | 全过 |
| 9 | PRD 第 10 节的 40 条验收标准 + 边界情况 | 8 | 全过 |
| 10 | 网页版（对外发布用，假扮真实域名测的） | 4 | 全过 |
| 11 | 第三版 · 添加栏 / 秒级用时 / 深浅切换 | 5 | 全过 |
| 12 | 第三版 · 首页金句 | 6 | 全过 |
| 13 | 第三版 · 账号与云同步 | 5 | 全过 |
| 14 | 第三版 · 后端自检与错误兜底 | 11 | 全过 |
| 15 | 第四版 · 金句分三种字体 | 3 | 全过 |
| 16 | 第四版 · 侧边栏收拾干净 | 2 | 全过 |
| 17 | 第四版 · 积分规则 | 6 | 全过 |
| 18 | 第四版 · 小铺货架与兑换 | 5 | 全过 |
| 19 | 第四版 · 装饰真正用上 | 4 | 全过 |

## 几个值得单独说的检查点

- **响铃时长**：真的等它响完，量到的是 10 秒出头（要求是"约 10 秒"），并且响完之后不会自己又响起来。
- **关页面期间时间到点**：关着页面的那段时间里如果时间到了，重新打开会立刻补响。
- **刷新页面时正在计时**：剩余时间按真实流逝的时间继续，不会白费，误差在 1.2 秒内。
- **备份是真的写进文件了**：改一条数据 → 1 秒多之后把那个文件读出来，新内容确实在里面；文件是能看懂的纯文本。
- **从文件恢复**：先弹"会覆盖"的确认框，点取消完全不动现有数据；点确认之后任务、总结、随手记都回来了。
- **坏文件**：往备份文件里塞一段乱码再去恢复，App 会说不认识这个文件，现有数据一点没动。
- **跨零点**：计时挂在前一天时，过了零点会被收走，用掉的时间记在前一天。
- **窄窗口**：把窗口拖到 320 像素宽，没有横向滚动条。
- **很多任务**：一次加 30 条，页面不破版，顶部"完成 0 / 31"跟着更新。
- **超长任务名**：一条 40 多字的任务名，排版没被撑破。

## 修掉的三个真 bug

1. **响铃响完又自己响起来。** 原来响铃 10 秒结束后，计时器下一次刷新会把闹钟重新叫起来，导致一直响。测试量到 22 秒还在响，于是定位并修掉了。
2. **恢复之后那个面板没关。** 从文件恢复成功后，备份面板又自己弹出来挡住界面，你看不到恢复后的内容。改成恢复完直接让你看到数据。
3. **打开「回看」会凭空生成空白日期。** 日历要检查当月每天"有没有记录"，而那个检查函数写成了"先建一条空的、再看它是不是空的"——于是每打开一次回看，就给当月 30 天各留一条空白记录，还写进了备份文件（你那个备份文件里那 30 个空日期就是这么来的）。

   修法是把"查看"和"创建"分开：查看的永不新建，只有真正写入时才创建；并且打开 App 时会自动把以前遗留的空白日期清掉。

   这条专门补了两个测试（第 2 阶段和第 7 阶段各一个）。我还验证过它们**真的能抓住这个 bug**：把修复退回原样后，测试立刻变红，报的正是"凭空多出 30 个任务日期"。

## 一件老实交代的事

浏览器弹出的"选保存位置 / 选文件"那个系统窗口，没法用程序自动点。所以测试里把这两个**系统弹窗**替换成了浏览器自己的一个真实文件，**其余全部是真实代码路径**——真的写文件、真的读文件、真的恢复。

也就是说：自动测试盖住了这一步之外的所有逻辑，而"点开系统窗口选一个文件"这个动作需要你亲手验一次。

**请你手动验一次（一分钟）：**

1. 双击 `index.html`
2. 点左下角那条状态 → 点「选择备份文件的位置」
3. 在弹出的窗口里选到 `going的app` 这个文件夹，文件名填 `今天备份.json`，保存
4. 看左下角是不是变成了"已自动保存到文件 · 今天备份.json · 现在的时间"
5. 顺便在文件夹里看看，`今天备份.json` 是不是出现了

如果第 4、5 步都对，那整个备份链路就是通的。

## 怎么自己再跑一遍

双击文件夹里的 **运行测试.bat**，它会跑完 114 项并把结果打在窗口里。测试会自己开一个干净的浏览器环境，**不会碰你平时用的数据**。

## 第四版（外观 + 积分乐园）是怎么测的

第 15—19 阶段，一共 20 项：

- **字体**：直接问浏览器"这段字到底是用哪个字体画的"（不是只看 CSS 里写了什么），并且把 161 条金句**逐条**过一遍，确认古诗用行楷、歌词用楷体、名言没被换掉；还量了最窄的窗口下不会被大字号撑破
- **侧边栏**：确认备份入口真的缩成了一个小按钮（量了高度、确认那两行字只剩 1 像素），点进去还是原来那个备份窗口
- **积分规则**：签到能拿 5 分、再点不再加、刷新还在；专注满 5 分钟自动 +3（4 分钟不给、40 分钟也只给一次）；完成满 5 个任务自动 +5；三件事拿满是 13 分；**还把页面时间挪到第二天**，确认新的一天可以重新拿
- **小铺**：23 件东西、四类货架、分数不够时换不了、够了才扣分、换到手永久归你、刷新还在
- **装饰**：摆件最多同时摆 3 个（第 4 个会被挡住并说清原因）、计时一开始角落的摆件跟着退场、换主题后整页底色和主色真的变了、底纹和花边能同时用也能各自收起来

> 顺带修掉一个测试自己的坑：App 在"页面要关了"时会保存数据，所以在 App 页面上直接清缓存会被它写回去，导致测试之间数据串味。现在清缓存前先离开 App 页面（`tests/helper.js` 里的 `freshState`）。

## 第三版的账号功能是怎么测的

后端（Cloudflare Worker）在测试时，是用**同一份代码**跑在本机的一个小服务器上的——也就是说，第 13 阶段那 5 项测的是真实的注册、登录、存取、冲突处理流程，只是把数据库换成了内存版。

**唯一没被测到的**：真实 Cloudflare 的 D1 数据库那一层（因为要联网部署才能跑）。所以部署完之后，**请你亲手注册一个账号试试**——那一步是我没法替你验的。

## 第 14 阶段：部署排错用的自检

部署账号功能时卡在「连不上服务器」，为此加了两样东西：

1. **一个自检页**：把 Worker 的网址直接粘到浏览器打开，它会自己检查三条——服务跑没跑起来、数据库绑定（变量名必须是 `DB`）有没有、四张表在不在。哪条不对，它就写出该怎么办。
2. **一句人话兜底**：服务器内部真出问题时，App 里显示的是**具体原因**，不再是笼统的「连不上服务器」。

这 7 项测试覆盖了：配置全对、没绑定、缺表、数据库连不上、注册报错能读懂、绑定配好能注册成功，以及**"测试本身不会偷偷跑去连线上服务器"**这一条。

> 顺带发现并修掉一个真问题：`config.js` 填上真实网址之后，测试页面会转头去连线上服务器，导致第 13 阶段 5 项全部失败。现在测试里强制用本机服务器，第 13 阶段恢复全过。

## 部署时真碰上的两个坑（都已修，并各配了一条测试）

**坑一：数据库绑定没跟着部署。** 加完绑定必须再点一次「部署」，否则不生效。现在后端有个自检页，打开 Worker 网址就能看到绑定到底有没有。

**坑二：密码加密的参数撞了两条平台限制。** 原来写的是 120000 次迭代，结果：

- Cloudflare 规定最多 **100000** 次，写多了直接报错（注册时那句 `Pbkdf2 failed...` 就是它）
- 免费版每个请求只给 **10 毫秒 CPU**，而本机实测 100000 次要 18.5 毫秒 —— 就算平台不拦，也会被 CPU 掐掉

实测数据（同一套浏览器加密库）：100000 次 ≈ 18.5ms、50000 次 ≈ 9.5ms、**20000 次 ≈ 3.7ms**、10000 次 ≈ 1.9ms。现在取值 **20000**。测试里会把 Cloudflare 的限制搬过来跑一遍注册流程，并卡死"不许超过 20000"，改大之前必须先重新量。

**坑三：错误提示把两件事混成了一句话。** 原来不管什么毛病，弹出来的都是"连不上服务器"，于是"网络没通"和"服务器回话了但页面自己出错"长得一模一样，排查全靠猜。现在分开：

- 请求没通 → 「连不上服务器（Failed to fetch）」——带上底层那句英文
- 服务器回话了 → 页面处理时的毛病会单独报「服务器回话了，但页面处理时出错了：…」

两条各配了一个测试（第 14 阶段）：一条确认"网络不通时能说出底层原因"，一条**故意让页面出错**、确认它不会被误报成"连不上服务器"。

**定位用的工具：`账号连接诊断.html`**

连不上服务器时，双击它 → 点「开始检查」。它会依次试：读配置、本机加密速度、最简单的 GET、和 App 一样发法的 POST、跳过预检的 POST、真的注册一个临时账号，然后把整份结果写成一段文字放到下面的框里，**整段复制发给 Codex** 就能定位。这个页面本身也有一条测试盯着，保证它不是坏的。
