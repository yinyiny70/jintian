# Git 怎么用（给这个项目）

## 现在的状态：已经建好了，第一次存档也做完了

```
分支：main
存档：1 个
  d3a9697  第一版：今天 App 完成，61 项测试全过
```

这一版，32 个文件，包含 App 本体、全部文档和 61 项测试。

日常你**什么都不用做**。想自己操作的时候，看下面。

## 以后想留个存档点

双击 **`保存版本.bat`**。

黑窗口会问你一句：

```
这次改了什么？（直接回车 ＝ 日常存档）：
```

随便写一句（或者直接按回车），它会存下来，然后列出最近的存档点。

**建议的习惯**：每次让我改完东西、你自己确认没问题之后，就双击存一次。这样你手上永远有一个"上次还好好的"版本可以退回去。

## 你的名字和邮箱

第一次存档的署名我先填了占位符：`me <me@local>`——因为你还没告诉我你想用什么名字。

这两项只写进**你自己电脑上**的提交记录，不会发给任何人。想改的话：

- **告诉我你的名字和邮箱**，我帮你把署名改过来（包括已经做好的这一条）；
- 或者你自己双击一次 `初始化Git仓库.bat`，它会问你，然后把以后的署名定下来。

## 这套东西能给你什么

- 每一次改动都有一个存档点，出问题能退回去。
- 任何时候能看清楚"这两天到底动了什么"。
- 万一某次改坏了，不用重做，退回上一个存档点就行。

想退回去、想看某次改了什么，**直接跟我说就行，我帮你操作。你自己不需要记任何 git 命令。**

## 万一报错

如果双击后看到黑色窗口里出现 `[×] 出错了`：

1. 先把窗口内容截图
2. 把截图发给我

就这样，不用自己动手修。

## 附：这套按钮是怎么回事

`初始化Git仓库.bat` 和 `保存版本.bat` 本身只是启动器，真正的逻辑在旁边的 `tools/` 文件夹里（两个脚本加一个共用文件）。之所以绕这一层，是因为 cmd 自己处理中文批处理会乱码，而 Node 处理中文没有问题。你完全不用打开那个文件夹。
