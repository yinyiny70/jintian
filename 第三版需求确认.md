# 第三版需求确认（待你点头）

> 这份是"要做成什么样"的约定。你确认之后我才动代码。
> 有问题的地方直接说，改这份文件比改代码便宜得多。

---

## 五条需求，逐条说清

### 1. 添加任务时不再填预估时长

**现在**：添加栏是 `[今天还要做什么…] [30] 分钟 [添加]`，加一条任务要填两样。

**改成**：`[今天还要做什么…] [添加]`——只写事情名字，回车就行。

**但"预估时长"这个概念保留**，只是挪个地方：

- 要设预估：点任务行上的**笔形图标**，在编辑状态里同时改名字和预估
- 任务行照样显示 `预估 45 分钟 · 实际 31 分钟`
- 超过预估照样标 **（超了）**——这个提示不删，它是用来告诉你"这件事你估得不准"
- **没设预估的任务**，点「开始」时弹框默认填 **25 分钟**

---

### 2. 单件事的用时精确到秒

| 位置 | 显示成 |
|---|---|
| 今日计划 · 任务行 | `实际 38 分 12 秒` |
| 备忘录 · 任务感受 | `38 分 12 秒` |
| 回看 · 当天做的事 | `48 分 05 秒` |
| 首页 · 今天专注 | `1 小时 24 分`（**不变**） |
| 回看 · 当天专注 | `4 小时 23 分`（**不变**） |

不足 1 分钟的显示成 `45 秒`。总时长保持到分——这是你定的。

---

### 3. 首页加"一句会变的话"

**内容**：古诗、古文名句、中外名言，**以及流行歌词**（你已知情歌词的版权风险，选择保留）。

**数量**：150–200 条，**内置在 App 里，不联网**。洗牌后顺序播放，一轮放完重新洗牌。
10 分钟一条，一天有 144 个时段，所以这样安排基本上一天之内不会撞车。

**节奏**：每 **10 分钟**自动换一条，切换时淡出淡入。以 10 分钟为一个时间片（比如 10:00–10:09 是同一句），
所以同一时刻不同设备看到的是同一句。

**位置**：首页**中间偏上，大字**。

**任务一开始计时，首页只显示倒计时**：

```
读 30 页书          ← 任务名
18:42              ← 大号剩余时间
▬▬▬▬▬░░░░░         ← 细进度线
[暂停] [结束]
```

金句和"今天专注"**都退场**。计时结束回到首页，它们再回来。

**这条会推翻一条旧规矩**：最初你要求"首页只放两组信息"，还专门写了测试盯着它。
现在首页变成"没计时：金句 + 此刻 + 今天专注；计时中：只有倒计时"。PRD 和那条测试都会跟着改——
这是你改的主意，不是我做错了。

---

### 4. 背景改成阳光的浅色

**根本原因**：App 现在是**跟随系统深浅色**的。你的电脑是深色模式，所以它跟着变黑了。

**改成**：

- App **不再跟随系统**，**默认固定用"阳光浅色"**
- 底色换成**暖米黄 / 奶油色**，配柔和的暖色光斑（杏色、蜜色），不做死板的纯黄
- 深色那一套**保留**，但改成**偏暖**的深咖／暖黑，不再是现在的深灰绿
- 侧边栏底部加一个小切换：**浅色 / 深色 / 跟随系统**，默认浅色

**一个技术上的取舍，先打预防针**：玻璃要看出"通透"，背景得有明暗层次。背景太白太浅，
玻璃就糊成一片白、退化成普通卡片。所以浅色底不会做成纯白，会保留一点点暖灰的层次。
**实装之后你看效果，觉得不够阳光我再往亮里推**——这一项调起来很快。

---

### 5. 账号注册登录 + 云端同步

**这是这一版最大的一件事**，它把项目从"一个网页"变成"一个有服务器的系统"。

| 决定 | 内容 |
|---|---|
| 后端 | Cloudflare Workers + 数据库（免费额度对个人和朋友几个账号绰绰有余） |
| 谁能注册 | 开放注册，朋友也能各自注册 |
| 注册要什么 | **用户名 + 密码**，不要邮箱 |
| 数据 | 每人一份，互相看不到 |
| 本机版 | **也要登录并同步**（带本地缓存：断网也能记，联网自动补传） |
| 同步方式 | 你确认过"一次只在一台设备上用"，所以打开时先比对谁更新、再上传，不做复杂合并 |
| 忘记密码 | **找不回来**（你选的不要邮箱）。兜底是你自己导出的备份文件 |
| 首次登录 | 如果本机已经有记录，会问一句"要不要把本机的记录传上来" |
| 退出登录 | 侧边栏底部加一个入口 |

**你必须知道的一个代价**：你的每日总结、给每个任务写的感受，会**离开你的电脑、存到服务器上**
（服务器是你自己的 Cloudflare 账号）。现在这些东西永远不会离开你的浏览器。

**还有一件事要告诉你**：作为服务器的主人，你**技术上**能看到所有账号存在里面的数据。
朋友用的时候，建议你在注册页说明一句"数据存在本站的云端账号里"，让他们知情。

---

## 第三版要做的

**界面与显示（快，改动小）**

- [ ] 添加栏去掉预估时长输入框，只留名字 + 添加
- [ ] 预估时长改到任务行的编辑状态里设置
- [ ] 未设预估时，计时弹框默认 25 分钟
- [ ] 任务行、任务感受、回看的单件事用时，精确到秒
- [ ] 首页总时长、回看当天总时长，保持到分
- [ ] 背景默认改成阳光浅色（暖米黄），不再跟随系统
- [ ] 深色配色改成偏暖的深咖色
- [ ] 侧边栏底部加"浅色 / 深色 / 跟随系统"切换

**首页金句（中等）**

- [ ] 整理 150–200 条内容（古诗、古文名句、中外名言、流行歌词）
- [ ] 首页中间偏上显示大字金句
- [ ] 每 10 分钟自动换一条，带淡入淡出
- [ ] 计时中首页只显示倒计时；计时结束时金句回来
- [ ] 同步改 PRD 里"首页只放两组信息"那一条，和对应的测试

**账号与云同步（大，工作量大头）**

- [ ] Cloudflare 后端：注册、登录、取数据、存数据四个接口
- [ ] 密码加密存储（服务器上也看不到明文）
- [ ] 注册 / 登录界面
- [ ] 登录状态记住（下次打开不用重新输密码）
- [ ] 本机版也能登录；本地缓存 + 联网自动同步
- [ ] 首次登录时询问"要不要把本机已有记录传上来"
- [ ] 侧边栏底部"当前账号 / 退出登录"
- [ ] 简单的防滥用（限制注册和请求频率，保护你的免费额度）

**必须一起做的**

- [ ] 现有 67 项测试全部继续通过
- [ ] 新增测试：登录注册流程、云同步、金句轮换、秒级显示、主题切换
- [ ] 更新 PRD、外观说明、使用说明、项目状态

---

## 第三版不做的

- 邮箱验证、邮箱找回密码（你选的不要邮箱）
- 手机端专门适配（手机上能打开，但体验还是不如电脑）
- 多人协作、数据分享给别人看
- 实时协作／两台设备同时在线冲突合并（你确认过不会同时用）
- 第三方登录（微信、GitHub 登录等）
- 数据统计图表、周报月报
- 任务重复、分类、标签、优先级
- 换肤、自定义主题色
- 改成手机 App

---

## 建议的推进顺序

一次性全做完再交付，你要等很久才看到东西。我建议分三段：

| 顺序 | 内容 | 你什么时候能看到效果 |
|---|---|---|
| 第一段 | 第 1、2、4 条（添加栏、秒级显示、阳光浅色） | 很快，改动集中在样式和显示 |
| 第二段 | 第 3 条（金句） | 内容整理好就能看到 |
| 第三段 | 第 5 条（账号 + 云同步） | 需要几天，是这一版的大头 |

每段做完都跑一遍测试，然后你确认再往下走。

---

## 等你确认的三件小事

1. **浅色/深色那个切换要不要？** 我建议要——你现在正是被"跟随系统"坑了，有个手动开关以后不会再困惑。
2. **计时中"今天专注"也一起退场**（你选的①），确认一下。
3. **歌词的版权风险你已经知情并选择保留**，确认一下。
