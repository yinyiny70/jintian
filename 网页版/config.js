/* ============================================================
   部署相关的配置。改这里就够了，不用动代码。
   ============================================================ */
window.JINTIAN_CONFIG = {
  // 云同步的服务器地址（就是你部署出来的那个网址）。
  //   - 网页版：留空即可，它会自动用当前网址（同一个域名下的 /api）
  //   - 本机版：必须填上，否则本机那份同步不上去。
  //   例：apiBase: "https://jintian.你的名字.workers.dev"
  apiBase: "",
};
