# 今天 · 使用说明

## 一、怎么打开

**双击桌面上的「今天」图标。**

桌面上还没有这个图标的话，先双击一次本文件夹里的 `创建桌面快捷方式.bat`，它会给你放一个上去。

也可以用最原始的办法：双击本文件夹里的 **index.html**。

打开之后建议一直开着——它的价值就在于在你做事的时候盯着你。你可以把它拖到任务栏固定，下次一点就开。

它是纯本地网页，不需要联网，也不需要安装任何东西。断网照样用。

## 一之二、账号（可选，需要先部署后端）

侧边栏**左下角**有一栏写着「没有登录」。点它可以注册或登录一个账号。

登录之后：

- 你记的每一点东西都会**自动同步到云端**，换任何一台电脑登录同一个账号都能看到
- **断网也能继续记**，联网后自动补传
- 别人看不到你的记录，你也看不到别人的
- 不想用了就点「退出登录」，本机记录不会丢，只是不再同步

> 账号功能需要先部署一个免费的后端，步骤在 `怎么开启账号功能.md` 里。**还没部署的话，这一栏会写"云端地址还没配"，点进去会用不了**——本机的其他功能完全不受影响。

## 二、四个页面是干嘛的

| 页面 | 用来做什么 |
|---|---|
| **首页** | 只看两件事：此刻在不在计时（还剩多久）、今天一共专注了多久。别的什么都不放 |
| **今日计划** | 今天要做的事都在这里加。每条写个预估时长，可以拖动排顺序 |
| **备忘录** | 今天留下的所有文字：今日总结、随手记、给每件事写的感受 |
| **回看** | 点日历上的某一天，回到那一天看当时做了什么、花了多久、写了什么 |

计时没有单独一页——它长在每件事上。

## 三、一天的用法

**早上**：进「今日计划」，把今天想做的事一条条加上，每条顺手填个预估时长，拖动排一下先后。

**做事前**：点这件事右边的「开始」，填这次打算做多久（默认给的是你写的预估时长，可以改），开始倒计时。

**做的过程中**：页面放在那儿就行。底部一直显示还剩多少，切到别的页面也看得见。中途要接电话、要停一下，点「暂停」。

**时间到了**：响铃约 10 秒，同时弹出一个框问你三件事之一：

- **做完了** → 接着写一句"这件事感觉怎么样"，然后打勾完成
- **再加 N 分钟** → 继续倒计时，之前用掉的时间照样算
- **先停下** → 结束这次计时，不打勾，用掉的时间照样算

不管选哪个，这段时间都记在这件事头上，并计入今天的专注时长。

**任何时候想到什么**：切到「备忘录」，在"随手记"里写一句，回车就存下了。

**晚上**：在「备忘录」上面那个框里写今天的总结。

**以后**：在「回看」里点某一天，那天的任务、用时、感受、总结、随手记都在右边。

## 四、备份（这段最重要，请一定看）

**第一次打开**时，它会请你选一个备份文件的位置。建议就选在这个文件夹里，文件名用 `今天备份.json`。

选好之后，**你改的每一点东西，1 秒内都会自动写进那个文件**。左下角那条状态会显示最后一次写入的时间，你随时能看到它在工作。

**万一哪天数据不见了**（比如你在浏览器里点了"清除浏览数据"，或者换了电脑）：

1. 打开 App
2. 点左下角那条状态
3. 点「从文件恢复…」
4. 选你之前那个 `今天备份.json`
5. 确认覆盖 → 全部回来

这个文件是纯文本，你能用记事本打开看懂里面写了什么。所以就算哪天 App 出问题，你的记录也跑不掉。

浏览器出于安全考虑，**偶尔会再问你一次"允许写这个文件吗"**，点允许就行，这是正常的。

## 五、五件你需要知道的事

1. **页面必须开着才会响铃。** 浏览器关了、电脑关机或睡眠，它不会响，也不会在后台替你计时。不过如果你关着页面的这段时间里时间到了，下次打开它会立刻补响。
2. **只认这台电脑的这个 Edge。** 换个浏览器就看不到数据了，那时候用备份文件恢复。
3. **清除浏览器数据会清掉本地数据。** 靠备份文件兜底。
4. **每天都是新的一页。** 昨天没做完的事不会自动跑到今天来；想继续做，自己重新加一遍。昨天的内容一个字都不会丢，在「回看」里。
5. **日历目前只显示当月**，还不能翻到上个月。

## 六、以后想改怎么办

- 想改文案、颜色、布局、加功能 → 直接跟我说，改的是 `app.js` 和 `styles.css`。
- **改代码不会动你的数据。** 数据不在这些文件里，在浏览器里和你那个备份文件里，所以随便改。
- 万一以后要改数据的格式，数据里带了版本号，我会写好转换，你不用手动整理。
