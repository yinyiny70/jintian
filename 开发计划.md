# going的app · 开发计划

> 依据：[PRD.md](PRD.md)。PRD 说"做什么"，这份说"按什么顺序做、每一步做完怎么验"。
> 有冲突时以 PRD 为准。

**执行方式**：我写，你验。每个阶段末尾我会告诉你"现在可以去试哪几条"，你照着 PRD 第 10 节里对应编号的条目点一遍就行。

---

## 一、技术选型（先定死，后面不再来回改）

| 决定 | 选择 | 理由 |
|---|---|---|
| 形态 | 纯前端，一个网页 + 一个样式文件 + 一个脚本文件 | 双击就能开，不用装任何东西，不用启动服务 |
| 框架 | **不用任何框架和第三方库，全部手写** | 你的电脑和我的环境都没有外网，装不了包；而且这个体量用不上框架 |
| 数据存放 | 浏览器自己的本地存储 | 满足"刷新、关闭、重启都不丢"，且打开是瞬间的 |
| 备份 | 浏览器写文件的能力，写到你自己指定的一个文件 | 满足"自动存一份到电脑上" |
| 提示音 | 浏览器现场合成，不用音频文件 | 少一个文件，少一处出错 |
| 界面 | 原生 HTML + CSS，中文，浅色为主并跟随系统深浅色 | 和已确认的草稿保持一致 |

**关键取舍**：不做编译、不打包。改成"打开 `index.html` 就能用"。代价是我们放弃了一些工程上的便利，换来的是你随时能打开、随时能看、坏了也容易修。

---

## 二、文件结构

最终会长成这样（现在还没有代码文件）：

```
going的app/
├── index.html          ← 双击这个就是 App
├── styles.css          ← 外观
├── app.js              ← 全部逻辑
├── PRD.md              ← 产品说明书（已有）
├── 开发计划.md          ← 这份（已有）
└── 文件说明.md          ← 索引（已有）
```

**一个要提前知道的事**：你的数据**不在这些文件里**。数据在浏览器的本地存储和你的备份文件里。所以以后更新代码（覆盖 `app.js`）**不会**弄丢你的数据。

---

## 三、开发阶段

十个阶段，按顺序做。每个阶段都必须先验收再进下一个——尤其是阶段 0。

### 阶段 0 · 技术验证（最关键，必须第一个做）

**要做什么**

先写一个一次性的测试页（用完就删），在你自己的 Edge 里挨个试下面五件事。这些决定了后面所有方案，猜不如试。

| 编号 | 要验证的事 | 如果不行会怎样 |
|---|---|---|
| T1 | 双击本地网页后，能不能把数据存在浏览器里，关掉重开还在不在 | 整个数据方案要重做 |
| T2 | 同样情况下，更可靠的存储方式能不能用（两者选其一） | 退而用 T1 的方式 |
| T3 | 能不能弹出"选择保存位置"的窗口、并持续自动往那个文件里写 | 备份得改成手动导出 |
| T4 | 把页面切到后台、或者电脑休眠一段时间，计时还准不准 | 决定倒计时怎么算 |
| T5 | 把这个网页挪到别的文件夹，数据还在不在 | 决定代码文件能不能随便挪 |

**做完的标志**：一张结论表，写清每一项"能用 / 不能用"，以及最终选定的存储和备份方案。

**这一阶段结束你要做的事**：什么都不用，等我把结论拿给你看。如果 T3 不成立，我会带着备选方案回来问你。

**大小**：小

---

### 阶段 1 · 骨架与导航

**要做什么**

- 建出 `index.html` 的外壳：左边固定侧边栏，右边内容区
- 侧边栏顶部放 App 名和今天日期，底部放备份状态条（先写死文字）
- 四个入口能点，点了能切换页面（页面先都是空占位）
- 建立配色方案，浅色 + 深色两套，跟随系统
- 窄窗口时侧边栏自动变成顶部一行

**做完的标志**：四个空页面能来回切，窗口拉窄拉宽都不破版。

**这一阶段结束你要做的事**：试 **PRD 第 10 节的 39、40**（窄窗口不破版、拉宽不留空档）。

**大小**：小

---

### 阶段 2 · 数据层（本地持久化）

**要做什么**

- 定下数据的样子：日期 → 当天有哪些任务、每条的记录、当天的总结、当天的随手记
- 数据里带一个**版本号**，以后改格式时老数据还能读出来
- 打开页面先把数据读进来；任何改动都在 0.3 秒后自动存一次
- 数据出问题时不要白屏，要给出看得懂的提示

**做完的标志**：随便改点东西，刷新页面、关掉浏览器、重启电脑，数据都还在。

**这一阶段结束你要做的事**：试 **A1 ~ A4**。

**大小**：中

---

### 阶段 3 · 今日计划

**要做什么**

- 添加任务（任务名 + 预估时长，回车或点按钮）
- 勾选完成 / 取消勾选；完成的行加删除线、去掉"开始"
- 拖动调整顺序
- 改名、删除
- 任务行显示 `预估 45 分钟 · 实际 31 分钟`，超了显示"（超了）"
- 页面顶部显示 `9月21日 星期一 · 完成 2 / 5`

**做完的标志**：这个页面已经完全可用，即使计时还没做。

**这一阶段结束你要做的事**：试 **B5 ~ B10**。

**大小**：中

---

### 阶段 4 · 计时与提醒（整个 App 的核心）

**要做什么**

- 每个未完成任务上一个"开始"按钮
- 点开始弹出输入时长的框，默认填这条任务的预估时长
- 倒计时按**目标结束时刻**计算，而不是每秒减一（这样切后台、电脑卡顿都不会跑偏）
- 计时中：任务行高亮显示剩余时间，底部出现计时条
- 暂停 / 继续 / 中途结束
- 到点：响铃约 10 秒 + 弹出响铃框
- 响铃框三选一：做完了（接一步写感受并打勾）/ 再加 N 分钟 / 先停下
- **不管怎么结束，用掉的时间都记账**
- 刷新页面后，正在跑的计时自动接着走
- 同一时间只允许一个计时，冲突时给出提示
- 电脑休眠后回来，如果已经越过结束时刻，立刻补响

**做完的标志**：可以完整地"排一件事 → 计时做完 → 打勾 → 留下感受"。

**这一阶段结束你要做的事**：试 **C11 ~ C21**（这是最长的一组，建议一次试完）。

**大小**：大

---

### 阶段 5 · 首页

**要做什么**

- 没有计时时显示"没有在计时"+ 一条去今日计划的链接
- 计时中显示任务名、剩余时间、一条细进度线
- "今天专注"按秒实时增长
- 保证页面上**只有这两组信息**，不加任何别的东西

**做完的标志**：抬眼就能知道现在什么状态。

**这一阶段结束你要做的事**：试 **D22 ~ D24**。

**大小**：小

---

### 阶段 6 · 备忘录

**要做什么**

- 今日总结：一天一篇，随写随存，旁边显示"已自动保存 · 时间"
- 随手记：输入框 + 回车加一条，自动带上当前时分；可以删
- 任务感受：只列出**今天有用时记录**的任务，就地写一句话
- 三块从上到下：总结 / 随手记 / 任务感受

**做完的标志**：一天的文字都能在这里落下，且分得清哪句属于哪。

**这一阶段结束你要做的事**：试 **E25 ~ E28**。

**大小**：中

---

### 阶段 7 · 回看（日历）

**要做什么**

- 当月日历，有记录的日期带小圆点，今天有单独标记
- 点任意一天，右侧换成那天的内容：做的事（含用时和感受）、当天专注时长、当天总结、当天随手记
- 过去的内容**只读**，点今天则提示去别处改
- 没有记录的日期给一个明确的空状态，不是一片空白

**做完的标志**：能把过去任意一天完整地翻出来看。

**这一阶段结束你要做的事**：试 **F29 ~ F33**。

**大小**：中

---

### 阶段 8 · 备份与恢复

**要做什么**

- 第一次使用时，引导你选一个备份文件的位置
- 每次数据变化后自动写一次；页面关闭前再写一次
- 侧边栏底部状态条显示最后一次成功写入的时间
- 点状态条打开小面板：换位置 / 立刻备份 / 从文件恢复
- 恢复前必须有一次"会覆盖当前全部数据"的确认
- 浏览器偶尔要求重新确认授权时，界面上要解释清楚，别让你以为坏了

**做完的标志**：清除浏览器数据后，能用备份文件把一切拿回来。

**这一阶段结束你要做的事**：试 **G34 ~ G38**（第 37 条会真的清一次数据，做之前我会提醒你先备份）。

**大小**：大

---

### 阶段 9 · 整体走查与打磨

**要做什么**

- 把 PRD 第 10 节的 **40 条**从头到尾跑一遍，一条一条打勾
- 补边界情况：跨零点换页、空状态、特别长的任务名、特别多的任务
- 和已确认的设计草稿逐页比对，确保没有走样
- 深色模式单独看一遍

**做完的标志**：40 条全过，没过的要么改掉，要么在 PRD 里写清为什么不做。

**这一阶段结束你要做的事**：这是你的验收关。我会把结果表给你，你自己再挑几条抽查。

**大小**：中

---

### 阶段 10 · 交付

**要做什么**

- 确认 `going的app` 里文件齐全、`index.html` 双击可用
- 更新 `文件说明.md`，写清怎么用、怎么备份、怎么恢复
- 写一份"以后想改怎么办"的简短说明，并附上你说的那个备份文件在哪

**做完的标志**：一个不写代码的人，拿着这个文件夹能自己用起来。

**大小**：小

---

## 四、几件必须一开始就想清楚的事

这几条不属于某一个阶段，但一旦开始就来不及回头，所以列在这里。

| 事项 | 决定 | 为什么 |
|---|---|---|
| 倒计时怎么算 | 记"该结束的时刻"，每秒算还剩多少 | 每秒减一会被浏览器后台降频拖慢，计时就不准了 |
| 电脑休眠怎么办 | 回到前台时立刻核对一次，越过了就补响 | 休眠时不响是浏览器的限制，能补上就不算丢 |
| 跨零点怎么算 | 以自然日为准，过了零点换新的一页 | PRD 附录 B 已确认 |
| 数据带版本号 | 每份数据头部写一个版本号 | 以后改格式，老数据还能迁过来，不用你手动整理 |
| 只有一个计时 | 状态里只存一个正在跑的计时 | 避免"两个任务同时计时"这种算不清的情况 |
| 修改代码不动数据 | 数据和代码分开存 | 你随时可以让我改界面，不用怕数据没了 |

---

## 五、风险与应对

| 风险 | 会怎样 | 应对 |
|---|---|---|
| 本地网页用不了"自动写文件"的能力 | 自动备份做不成 | 阶段 0 先验证；不行就退成"点一下导出"，我会先来问你 |
| 浏览器切后台时计时不准 | 到点不响 | 用结束时刻计算 + 回到前台补响 |
| 清除浏览器数据 | 本地数据没了 | 自动备份文件兜底，并在界面上提醒风险 |
| 数据格式以后要改 | 老数据读不出来 | 数据带版本号，写迁移逻辑 |
| 文件挪了位置 | 担心数据丢 | 阶段 0 的 T5 会验证，并在说明书里写清 |
| 需求中途变 | 返工 | 改需求先改 PRD 那一节，再动代码 |

---

## 六、进度总览

| 阶段 | 内容 | 大小 | 依赖 |
|---|---|---|---|
| 0 | 技术验证 | 小 | 无（必须第一个做） |
| 1 | 骨架与导航 | 小 | 0 |
| 2 | 数据层 | 中 | 0 |
| 3 | 今日计划 | 中 | 1、2 |
| 4 | 计时与提醒 | 大 | 2、3 |
| 5 | 首页 | 小 | 4 |
| 6 | 备忘录 | 中 | 2 |
| 7 | 回看 | 中 | 2、3、6 |
| 8 | 备份与恢复 | 大 | 0、2 |
| 9 | 整体走查 | 中 | 全部 |
| 10 | 交付 | 小 | 9 |

阶段的先后不是死的：6、7、8 可以在 3 之后按你的兴趣换顺序。**唯一不能换的是 0 必须在最前面**，以及 9 必须在最后。

---

## 七、我们怎么配合

- 每做完一个阶段，我停下来，告诉你**现在去试哪几条编号**，你试完给一句话（过了 / 哪条不对）。
- 你觉得哪里不对味，随时打断，不用等阶段做完。界面和文案改起来很便宜。
- 每条 PRD 的验收标准我会自己先跑一遍，再交给你抽查，不把没试过的东西丢给你。
- 需求如果要变，先改 PRD 对应那一节，再改代码。这样文档永远是可信的。

---

## 八、马上要做的第一步

**阶段 0**。我会先写一个一次性的测试页丢进 `going的app`，你双击打开、按上面的提示点几下，我根据结果定下存储和备份方案，然后才开始写真正的代码。

这一步很短，但它决定了后面九个阶段顺不顺。
