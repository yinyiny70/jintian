# 怎么开启账号功能（Cloudflare，免费）

## 先说清楚这是什么东西

账号和云同步需要一个"后端"——一个跑在网上的小程序，负责记住谁是谁、把记录存在服务器上。

我把它写成了**一个文件**（`server/worker.js`），配合 Cloudflare 的免费数据库（D1）。
**全程不需要在你电脑上装任何东西**，全部在网页后台点。

要花钱吗？**不要**。免费额度：每天 10 万次请求、5GB 存储。你和几个朋友用，连零头都用不到。

---

## 第一步：建数据库

1. 登录 Cloudflare → 左侧找 **Storage & Databases** → **D1 SQL Database** → **Create**
2. 名字填 **`jintian`** → Create
3. 进去之后切到 **Console** 标签
4. 打开项目里的 `server/schema.sql`，把**整个文件**内容粘进输入框 → 点 **Execute**

成功的话，你会看到它建好了 4 张表（users / sessions / userdata / ratelimit）。

---

## 第二步：建一个 Worker，把后端代码粘进去

1. 左侧 **Workers & Pages** → **Create** → 选 **从 Hello World 开始**
2. 名字填 **`jintian-api`** → **Deploy**
3. 进去后点 **Edit code**（编辑代码）
4. 把编辑框里原有的内容**全部删掉**
5. 双击项目里的 **`复制后端代码.bat`**（它会自动把整段代码放进剪贴板，不用手动全选）
6. 回到 Cloudflare 的编辑框，点一下，按 **Ctrl+V** 粘进去 → 点 **Deploy**（部署）

---

## 第三步：把数据库接到这个 Worker 上

1. 在这个 Worker 的页面里 → **Settings**（设置）→ **Bindings**（变量和机密）→ **Add** → 选 **D1 database**
2. **变量名必须填 `DB`**（大写，一个字都不能差）→ 数据库选刚建的 `jintian` → **Save**
3. **存完之后一定要再点一次「部署 / Deploy」。** 绑定要跟着一次新的部署才会生效，
   这是最容易漏的一步。
4. 回到 Worker 首页，复制它的网址，长这样：

```
https://jintian-api.你的名字.workers.dev
```

---

## 第三步半：自检（强烈建议做，10 秒）

把上面那个网址**直接粘到浏览器地址栏打开**。会看到一个中文的自检页，帮你把三条都检查一遍：

```
✅ Worker 本身跑起来了
✅ 数据库绑定（变量名必须是 DB）
✅ 数据库连得上、表建好了
```

如果有一条是 ❌，它下面会直接写**该怎么办**（比如"没找到叫 DB 的绑定"）。

> **出任何问题，先把这一页截图发给 Codex**，比猜快得多。

---

## 第四步：把这个网址填进 App

打开项目里的 `config.js`，把地址填进 apiBase：

```js
window.JINTIAN_CONFIG = {
  apiBase: "https://jintian-api.你的名字.workers.dev",
};
```

然后双击 **`发布到网上.bat`**，重新发布一次（网页版才会拿到新地址）。

> 这一步你也可以**把网址发给我**，我帮你填好、顺手更新网页版文件夹。

---

## 怎么知道成功了

1. 先在浏览器里打开 Worker 那个网址，看到**「一切正常，可以回 App 里注册或登录了」**
2. 打开 App，左下角点 **「没有登录」**
3. 起个用户名（3–20 位字母数字）和密码（至少 6 位）→ 点 **「注册一个新账号」**
4. 左下角变成你的用户名，就成功了
5. 换一台电脑（或换一个浏览器）打开同一个网址，登录同一个账号 → **记录都在**

---

## 出问题时的对照表

| 现象 | 多半是什么原因 | 怎么办 |
|---|---|---|
| 注册时提示「连不上服务器」 | 地址填错了，或者 Worker 没能跑起来 | 把 config.js 里那个网址直接粘到浏览器打开，应该能看到**自检页**；打不开就是网址有问题 |
| 注册时提示「服务器内部出错」 | 数据库绑定没配好，或者配好没重新部署 | 看自检页：它会告诉你「没找到叫 DB 的绑定」，照着做 |
| 自检页说「缺这几张表」 | 第一步的建表没跑成功 | 回第一步的 Console，把 `server/schema.sql` 重跑一次 |
| 自检页说「连数据库时报错」 | 绑定选错了数据库，或者数据库被删了 | 回第三步重选一次 `jintian` |
| App 里改了代码还是老样子 | 改完没点「部署」 | 每次改完代码都要点一次 Deploy |
| 网页版注册不了 | 网页版还没拿到新的 config.js | 双击一次 `发布到网上.bat` |
| 一台电脑上有记录、另一台上没有 | 那台电脑没登录，或者没网 | 看左下角是不是显示你的用户名 |

**卡住就把报错截图发我。**

---

## 关于安全和隐私（你要知道的）

- **密码在服务器上是加密存的**（PBKDF2 加盐哈希）。就算数据库被人拿走，也看不出原密码。代价是：**密码忘了就找不回来**，我也解不开。
- 同一个用户名不能注册两次；连续输错会暂时被限流（防止别人暴力试）。
- 每次请求都要带登录凭证，别的账号读不到你的数据。
- 但有一点必须说清楚：**作为这个服务器的主人，你技术上能看到所有账号存在里面的数据。**
  朋友注册的时候，最好在注册页说明一句"数据存在本站的云端账号里"，让他们知情。
